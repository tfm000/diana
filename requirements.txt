# Web dashboard
streamlit>=1.40.0

# Text extraction
PyMuPDF>=1.23.0
EbookLib>=0.18
beautifulsoup4>=4.12.0
lxml>=4.9.0
markdown>=3.5

# TTS engines
kokoro-onnx>=0.4.0
soundfile>=0.12.0
piper-tts>=1.0.0

# Audio processing
pydub>=0.25.1
audioop-lts>=0.2.1; python_version>="3.13"

# Windows native TTS (WinRT) — Windows-only C-extensions; platform-gated so
# macOS/Linux installs and the 3.10-3.13 CI matrix never try to build them.
winrt-Windows.Media.SpeechSynthesis>=3.2.1; sys_platform == "win32"
winrt-runtime>=3.2.1; sys_platform == "win32"
winrt-Windows.Storage.Streams>=3.2.1; sys_platform == "win32"
winrt-Windows.Foundation>=3.2.1; sys_platform == "win32"

# Configuration
pyyaml>=6.0

# Platform paths
platformdirs>=4.10.0

# HTTP client (news scraping, ElevenLabs API)
requests>=2.31.0

# LLM providers (used for text cleaning and news summarization)
openai>=1.30.0
anthropic>=0.25.0
claude-code-sdk>=0.0.10
google-generativeai>=0.5.0
