---
phase: 04-engine-management-voice-catalog
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - diana/tts/install_state.py
  - diana/tts/kokoro_engine.py
  - diana/dashboard/pages/5_Settings.py
autonomous: false
requirements: [ENGINE-02, VOICE-07]
user_setup: []

must_haves:
  truths:
    - "User can uninstall an installed Piper voice from the UI, confirmed, with freed space shown before deletion (D-16)"
    - "Uninstall is blocked while the voice is a current per-job choice or a per-engine default — the user is told to switch first (D-17)"
    - "User can remove a single interrupted download's .part file, and a bulk 'Clean up partial downloads' clears all orphaned .part files at once (D-18)"
    - "The Kokoro model downloads on demand through the same generic layer (disk-check + resumable + md5 + atomic install), replacing the wget-hint error (D-19)"
    - "The large Kokoro asset (>200 MB) shows an explicit footprint confirm before download (D-04); small Piper voices stay one-click"
    - "native_os shows nothing to download/uninstall (OS-owned)"
  artifacts:
    - path: "diana/tts/install_state.py"
      provides: "in-use-block predicate (reads jobs.tts_voice + tts.default_voice.<engine> keys) + uninstall helper (delete .onnx + .onnx.json within model_dir) — VOICE-07/D-17"
      contains: "tts_voice"
    - path: "diana/tts/kokoro_engine.py"
      provides: "Kokoro asset URLs/sizes exposed for the in-UI download (D-19); the wget-hint error context updated"
      contains: "kokoro-v1.0"
    - path: "diana/dashboard/pages/5_Settings.py"
      provides: "Voices tab: uninstall (confirm + freed space + in-use block) + per-item Remove partial + bulk Clean up partial downloads + Kokoro model download row with footprint confirm (D-16/17/18/19/04)"
      contains: "Clean up partial downloads"
  key_links:
    - from: "diana/dashboard/pages/5_Settings.py"
      to: "diana.downloads.downloader.clean_partials"
      via: "bulk 'Clean up partial downloads' glob over model_dir (D-18)"
      pattern: "clean_partials"
    - from: "diana/dashboard/pages/5_Settings.py"
      to: "diana.tts.install_state"
      via: "in-use-block check before uninstall (D-17) + uninstall delete"
      pattern: "voice_in_use"
    - from: "diana/dashboard/pages/5_Settings.py"
      to: "diana.downloads.downloader.download_file"
      via: "Kokoro model download row reusing the generic layer with a D-04 footprint confirm"
      pattern: "download_file"
---

<objective>
Complete the phase with the manage/remove half (VOICE-07) and the Kokoro reuse that proves the layer is engine-generic (D-19): UI uninstall of an installed Piper voice (confirmed, freed space shown — D-16) that is blocked while the voice is in use as a per-job choice or per-engine default (D-17); per-item "Remove partial" plus a bulk "Clean up partial downloads" action for orphaned `.part` files (D-18); and routing the Kokoro model download through the same generic download/cache layer (disk-check + resumable + md5 + atomic install) with the D-04 threshold confirm on its large asset, replacing today's `wget`-hint error (D-19).

After this plan the full phase loop is closed: discover -> preview -> install -> use -> relabel -> uninstall/clean up, all in-app, with the SAME generic substrate driving both the Piper per-voice catalog and the Kokoro single-model download — exactly the D-19 boundary the phase exists to prove before heavy engines (Phase 5) reuse it.

Implements VOICE-07 (uninstall + in-use block + partial cleanup), D-16/D-17/D-18, D-04 (footprint confirm above the threshold), and D-19 (Kokoro via the generic layer). The downloader's `clean_partials` and the install-state probes shipped in Plan 02; this plan adds the in-use-block + uninstall helpers and the UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engine-management-voice-catalog/04-RESEARCH.md
@.planning/phases/04-engine-management-voice-catalog/04-PATTERNS.md
@diana/tts/kokoro_engine.py
@diana/database.py

<interfaces>
<!-- Substrate + DB facts for the in-use block and Kokoro reuse. -->

diana/tts/install_state.py (Plan 02; THIS plan adds):
  voice_in_use(db_path, engine, voice_id) -> str | None    # returns a reason string when blocked (D-17), else None
     # blocked if voice_id == any NON-TERMINAL job's tts_voice, OR == get_setting(db_path, f"tts.default_voice.{engine}", None)
  uninstall_piper_voice(voice_id) -> int                   # delete {id}.onnx (+ .onnx.json) under model_dir; return freed bytes
  (existing: piper_voice_installed, piper_footprint_bytes, kokoro_model_installed)

diana/downloads/downloader.py (Plan 02): download_file(...), has_space(...), clean_partials(directory) -> int

diana/database.py: get_setting/set_setting (148-167); list_jobs(db_path) (201-208) -> jobs with .status (JobStatus) and .tts_voice; delete_job unlink idiom (248-271)
diana/models.py: JobStatus terminal = {COMPLETED, FAILED}; non-terminal = PENDING/EXTRACTING/CLEANING/CHUNKING/SYNTHESIZING/MERGING (anything not terminal). The in-use block cares about non-terminal jobs (a pending/in-flight job still needs its voice file).
Phase-3 backstop already wired: resolve_default_voice validates a remembered id against the live list at selection time (registry.py:76-100) — D-17's up-front block is the protective layer on top.

diana/tts/kokoro_engine.py (MODIFY — D-19): the FileNotFoundError wget hint at lines 35-46 is the gap to close. Kokoro asset URLs (04-RESEARCH.md:497-501): kokoro-v1.0.onnx (~310 MB f32), .fp16.onnx (~169 MB), .int8.onnx (~88 MB), voices-v1.0.bin (28,214,398 bytes VERIFIED). Kokoro maps to an engine-level "model installed?" badge (single model, many baked-in voices — D-19/discretion), NOT per-voice rows. The download is UI-triggered (ENGINE-04) and lands in paths.model_dir() where the engine's configured paths already point (config.py:32-33). Per D-04 the f32 .onnx (~310 MB) crosses the >200 MB confirm threshold — show an explicit footprint confirm; pick a sensible default asset (int8 ~88 MB is the smallest viable) or let the user choose.

tests/test_uninstall.py (Plan 01 scaffold) defines the behavior: in-use block (job ref + default key arms), delete the pair, bulk *.part cleanup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: in-use-block predicate + uninstall helper; expose Kokoro assets for in-UI download</name>
  <files>diana/tts/install_state.py, diana/tts/kokoro_engine.py</files>
  <read_first>
    - diana/tts/install_state.py (the file being modified — add voice_in_use + uninstall_piper_voice in the cheap-probe lane)
    - diana/database.py (lines 148-167 get_setting; 201-208 list_jobs; 248-271 delete_job unlink(missing_ok=True) idiom — the delete pattern uninstall_piper_voice mirrors)
    - diana/models.py (JobStatus enum — terminal COMPLETED/FAILED vs non-terminal; the in-use block ignores terminal jobs)
    - diana/tts/kokoro_engine.py (lines 26-49 — the configured model/voices paths + the wget-hint FileNotFoundError at 35-46 to replace context for; the asset URLs)
    - tests/test_uninstall.py (Plan-01 scaffold: test_in_use_block both arms, delete pair, test_clean_partials)
    - 04-RESEARCH.md Pitfall 5 (D-17 in-use block reads job.tts_voice + tts.default_voice.<engine>), lines 497-501 (Kokoro asset URLs/sizes), 04-PATTERNS.md "File-cleanup idiom" + "kokoro_engine.py" sections
  </read_first>
  <behavior>
    - voice_in_use(db_path, engine, voice_id): returns a human reason string (e.g. "in use by a pending job" / "set as the {engine} default voice") when voice_id equals (a) the tts_voice of any NON-TERMINAL job in list_jobs, or (b) the stored tts.default_voice.<engine> value; returns None when free to remove (D-17)
    - uninstall_piper_voice(voice_id): unlinks {voice_id}.onnx and {voice_id}.onnx.json under paths.model_dir() (missing_ok=True, scoped to model_dir only); returns the freed bytes (sum of the sizes deleted) so the UI can show freed space (D-16)
    - Kokoro asset exposure: a small constant/helper (in kokoro_engine.py or install_state) giving the asset URL + filename + approx size so the UI can build the download without hardcoding URLs in the page
  </behavior>
  <action>
    In `diana/tts/install_state.py` add `voice_in_use(db_path, engine, voice_id)` and `uninstall_piper_voice(voice_id)` in the existing cheap-probe lane (filesystem + DB read only; lazy `from diana.database import get_setting, list_jobs` inside the function to keep the import light). uninstall mirrors delete_job's `Path(...).unlink(missing_ok=True)` and stays scoped to `paths.model_dir()` (never deletes outside the cache). The in-use check reads non-terminal jobs and the `tts.default_voice.<engine>` key (Pitfall 5).
    In `diana/tts/kokoro_engine.py` expose the Kokoro asset URLs + filenames + approximate sizes as a module-level constant or helper (per D-19, so the UI download row does not hardcode URLs). Update the wget-hint FileNotFoundError text so it points the user to the in-app Settings ▸ Voices download instead of a terminal `wget` (keep it raising if the model is genuinely absent — the UI downloads BEFORE synth; ENGINE-04). Do not otherwise change the engine's synth path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_uninstall.py -x -q 2>&1 | tail -8 && .venv/bin/python -c "src=open('diana/tts/install_state.py').read(); assert 'voice_in_use' in src and 'uninstall_piper_voice' in src; k=open('diana/tts/kokoro_engine.py').read(); assert 'kokoro-v1.0' in k; print('uninstall + kokoro assets present')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_uninstall.py` passes: a voice id matching a non-terminal job's tts_voice is blocked; a voice id matching the stored default key is blocked; an unreferenced installed voice is allowed and its `.onnx`(+`.onnx.json`) deleted; bulk `clean_partials` removes all `*.part` while non-`.part` files survive — the Plan-01 scaffold flips to GREEN
    - `uninstall_piper_voice` returns freed bytes and deletes only within `paths.model_dir()`
    - install_state.py still imports no heavy SDK; the DB import is lazy/inside-function
    - kokoro_engine.py exposes the asset URLs/sizes and the FileNotFoundError text no longer instructs a terminal `wget`
  </acceptance_criteria>
  <done>The in-use-block + uninstall helpers and Kokoro asset exposure land; the uninstall scaffold is green.</done>
</task>

<task type="auto">
  <name>Task 2: Uninstall + partial-cleanup + Kokoro download UI in the Voices tab</name>
  <files>diana/dashboard/pages/5_Settings.py</files>
  <read_first>
    - diana/dashboard/pages/5_Settings.py (the file being modified — the Voices tab from Plans 03/04/05; reuse dl_state/download machinery, badges, and the cross-engine browser rows)
    - diana/tts/install_state.py (voice_in_use, uninstall_piper_voice, kokoro_model_installed) + diana/downloads/downloader.py (clean_partials, download_file, has_space) + the kokoro asset constant
    - diana/dashboard/pages/1_Upload.py (lines 184-195 dismissible-block pattern) + 5_Settings.py:346-388 (the warn/success messaging idiom for a destructive/confirm action)
    - 04-RESEARCH.md D-16 (confirm + freed space), D-17 (block in-use), D-18 (per-item + bulk partial cleanup), D-04 (threshold confirm >200 MB), D-19 (Kokoro engine-level model row, NOT per-voice), Pitfall 5; 04-PATTERNS.md "Uninstall (VOICE-07, D-16/D-17/D-18)" section
  </read_first>
  <action>
    UNINSTALL (D-16/D-17): for each installed Piper voice row in the Voices tab, add an Uninstall control. On click, first call `voice_in_use(db, "piper", voice_id)`; if it returns a reason, show it and REFUSE ("This voice is {reason} — switch to another voice first"), do not delete (D-17). Otherwise show a confirmation step displaying the freed space (from `piper_footprint_bytes`/`uninstall_piper_voice`'s return) and, on confirm, call `uninstall_piper_voice` and flip the badge back to not-installed (D-16). native_os rows show no uninstall (OS-owned); Kokoro uninstall is the engine-level model (optional — at minimum Piper voices are uninstallable per VOICE-07).
    PARTIAL CLEANUP (D-18): for any row that has an orphaned `.part` in model_dir, show a per-item "Remove partial" action (unlink that one `.part`). Add a single "Clean up partial downloads" button that calls `downloader.clean_partials(paths.model_dir())` and reports how many were removed.
    KOKORO DOWNLOAD (D-19/D-04): add an engine-level Kokoro "model installed?" row (per the discretion note — single model, many baked-in voices, NOT per-voice rows) showing `kokoro_model_installed()` state + footprint. When not installed, a Download Model action runs the SAME generic flow as Piper: `has_space` pre-check (D-05), then the threaded `download_file` (Pattern 3 dl_state + st.fragment progress) for the chosen asset + voices-v1.0.bin into `paths.model_dir()`, md5/atomic install via the downloader. Because the f32 asset (~310 MB) crosses the D-04 >200 MB threshold, show an explicit footprint confirm before starting (default to the smaller int8 asset or let the user choose). After install, Kokoro flips to "Ready" and is usable with zero further setup — the wget hint is gone.
    Reuse the Plan-03 dl_state/thread/fragment machinery verbatim for the Kokoro download (do not duplicate it). Apply the None-safe discipline and keep heavy enumeration cached.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q 2>&1 | tail -4 && .venv/bin/python -c "import ast; ast.parse(open('diana/dashboard/pages/5_Settings.py').read()); s=open('diana/dashboard/pages/5_Settings.py').read(); need=['voice_in_use','uninstall_piper_voice','clean_partials','Clean up partial downloads','kokoro_model_installed','download_file']; missing=[x for x in need if x not in s]; assert not missing, missing; print('uninstall + cleanup + kokoro-download wiring present')"</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green; 5_Settings.py parses
    - Source wires: `voice_in_use` (checked BEFORE delete), `uninstall_piper_voice` (after confirm, freed space shown), a per-item Remove-partial action, a bulk "Clean up partial downloads" calling `clean_partials`, and a Kokoro model-download row using `kokoro_model_installed` + `has_space` + the threaded `download_file`
    - The uninstall handler refuses when `voice_in_use` returns a reason (D-17) and only deletes after an explicit confirm (D-16)
    - The Kokoro download reuses the Plan-03 dl_state/st.fragment machinery (no duplicated thread/poll code) and shows a footprint confirm for the large asset (D-04)
    - native_os rows expose no download/uninstall control (OS-owned)
  </acceptance_criteria>
  <done>Uninstall (confirm + in-use block + freed space), per-item + bulk partial cleanup, and the in-UI Kokoro model download all land in the Voices tab via the shared substrate; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify uninstall, partial cleanup, and the in-app Kokoro download</name>
  <files>diana/dashboard/pages/5_Settings.py</files>
  <action>
    Pause for human verification AFTER Task 2 — no new code is written here. It confirms the Streamlit-UX + real-network behaviors that cannot be unit-automated (04-VALIDATION.md "Manual-Only Verifications"): uninstall confirm + freed-space + in-use block, per-item + bulk partial cleanup, and the in-app Kokoro download with its D-04 footprint confirm replacing the wget hint. If the human reports a defect, fix it in 5_Settings.py / install_state.py / kokoro_engine.py and re-run before resuming.
  </action>
  <what-built>
    Uninstall for an installed Piper voice (confirmation + freed-space shown; blocked with an explanation while the voice is a current per-job choice or per-engine default), per-item "Remove partial" plus a bulk "Clean up partial downloads" action for orphaned .part files, and an in-app Kokoro model download (footprint confirm for the large asset; same byte-progress/disk-check/resume substrate as Piper) that replaces the old wget-hint error. native_os shows nothing to download/uninstall. (Streamlit-UX + real-network behaviors from 04-VALIDATION.md "Manual-Only Verifications".)
  </what-built>
  <how-to-verify>
    1. Launch `.venv/bin/python run.py`, open Settings ▸ Voices.
    2. UNINSTALL block (D-17): set a Piper voice as the per-engine default (or have a pending job using it), then try to Uninstall it — confirm it is REFUSED with an explanation to switch first.
    3. UNINSTALL (D-16): switch the default away, then Uninstall an installed Piper voice — confirm a confirmation step shows the freed space, and after confirming the voice's badge flips back to "downloads on first use" and the files are gone.
    4. PARTIAL CLEANUP (D-18): interrupt a download (Cancel mid-way to leave a .part), then use the per-item "Remove partial" to clear that one; create another orphan and use the bulk "Clean up partial downloads" button — confirm it reports the count removed and clears them.
    5. KOKORO DOWNLOAD (D-19/D-04): with the Kokoro model not installed, confirm the Voices tab shows a Kokoro model row with a footprint. Click Download Model — confirm a footprint confirm appears (D-04) for the large asset, then a byte-progress bar advances (same UX as Piper), and on completion Kokoro flips to "Ready". Then pick Kokoro on Upload and convert — confirm it works with no terminal/wget step.
    6. native_os: confirm native_os voices show browse/preview/label only — no download or uninstall control.
  </how-to-verify>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q 2>&1 | tail -3 && .venv/bin/python -c "import ast; ast.parse(open('diana/dashboard/pages/5_Settings.py').read()); print('syntax OK')"</automated>
    <human-check>Uninstall blocks in-use voices and confirms + shows freed space otherwise; per-item and bulk partial cleanup work; the Kokoro model downloads in-app with a footprint confirm and byte progress, then converts; native_os has no download/uninstall control.</human-check>
  </verify>
  <acceptance_criteria>
    - Full suite green and 5_Settings.py parses (automated check passes)
    - Human confirms: uninstall (block + confirm + freed space), partial cleanup (per-item + bulk), and the in-app Kokoro download (footprint confirm + progress + usable) all work; native_os shows no download/uninstall
  </acceptance_criteria>
  <resume-signal>Type "approved" if uninstall (block + confirm), partial cleanup (per-item + bulk), and the Kokoro in-app download all work, or describe what failed.</resume-signal>
  <done>Human-verified: uninstall + in-use block + freed space, per-item + bulk partial cleanup, and the in-UI Kokoro download (D-04 confirm) all work; native_os correctly has nothing to download/uninstall; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI delete action -> filesystem | Uninstall/cleanup delete files under the per-user cache |
| Diana -> GitHub release CDN | Untrusted Kokoro model bytes downloaded into the cache |
| in-use check -> DB/app_settings | Reads job rows + default keys to decide whether a delete is safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-FILE | Tampering | uninstall/cleanup deleting outside the cache | mitigate | uninstall_piper_voice and clean_partials operate ONLY within `paths.model_dir()` (basename-joined Paths, never user-supplied absolute paths); unlink(missing_ok=True) |
| T-04-INUSE | (correctness/availability) | deleting a voice an in-flight/pending job needs | mitigate | voice_in_use blocks delete when the id is a non-terminal job's tts_voice or a per-engine default (D-17); Phase-3 resolve_default_voice is the selection-time backstop |
| T-04-INT | Tampering | corrupt Kokoro model installed | mitigate | Kokoro download reuses the Plan-02 downloader: md5/Content-Range checks, `.part` -> atomic os.replace; voices-v1.0.bin size VERIFIED (28,214,398) |
| T-04-DISK | Denial of Service | large Kokoro model exhausting disk | mitigate | has_space pre-check gates the Kokoro download (the ~310 MB f32 asset is exactly why D-05 + the D-04 confirm exist) |
| T-04-SRC | Information disclosure / crash | Kokoro download thread calling st.* | mitigate | Reuses the Plan-03 dl_state/st.fragment pattern; thread writes shared state only |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — all deps pre-existing and pinned; RESEARCH Package Legitimacy Audit clean |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_uninstall.py tests/ -q` green (the uninstall scaffold runs live; full suite no regression).
- install_state.py exposes voice_in_use + uninstall_piper_voice (cheap lane, lazy DB import); kokoro_engine.py exposes the asset URLs and drops the terminal wget instruction.
- 5_Settings.py wires uninstall (in-use block + confirm + freed space), per-item + bulk partial cleanup, and the Kokoro model download (with D-04 confirm) via the shared substrate.
- The blocking human-verify checkpoint confirms the manage/remove + Kokoro-download behaviors per 04-VALIDATION.md Manual-Only list.
</verification>

<success_criteria>
- VOICE-07: UI uninstall (confirm + freed space, D-16) with the in-use block (D-17) and per-item + bulk partial cleanup (D-18).
- ENGINE-02 / D-19: the Kokoro model downloads on demand through the SAME generic layer (disk-check + resumable + md5 + atomic install), replacing the wget hint — proving the layer is engine-generic before Phase 5 reuses it.
- D-04: the large Kokoro asset shows a footprint confirm before download; small Piper voices stay one-click.
- native_os correctly exposes nothing to download/uninstall.
</success_criteria>

<output>
Create `.planning/phases/04-engine-management-voice-catalog/04-06-SUMMARY.md` when done. Record any deviation surfaced during the human-verify checkpoint and re-confirm the package-data flag for the Phase-6 packager.
</output>
