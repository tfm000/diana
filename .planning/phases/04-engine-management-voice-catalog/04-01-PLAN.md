---
phase: 04-engine-management-voice-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - diana/tts/base.py
  - tests/fixtures/voices_manifest.json
  - tests/test_downloader.py
  - tests/test_downloader_net.py
  - tests/test_catalog.py
  - tests/test_install_state.py
  - tests/test_voice_import.py
  - tests/test_voice_labels.py
  - tests/test_uninstall.py
autonomous: true
requirements: [VOICE-06]
user_setup: []

must_haves:
  truths:
    - "Full test suite collects GREEN with the new scaffolds present (no import errors, no collection failures)"
    - "Every Phase-4 symbol-dependent test is skipif-gated and flips to a live gate the moment its symbol lands — zero later test-file edits"
    - "TTSVoice carries a trailing-defaulted tags field so existing positional VOICES lists stay valid (D-14 storage substrate)"
    - "The network marker is registered so the opt-in real-download test does not warn"
  artifacts:
    - path: "diana/tts/base.py"
      provides: "TTSVoice with tags: tuple[str, ...] = () trailing-default field (A3 / D-14)"
      contains: "tags"
    - path: "tests/fixtures/voices_manifest.json"
      provides: "2-3-entry Piper voices.json excerpt incl. one multi-speaker voice"
      contains: "speaker_id_map"
    - path: "tests/test_downloader.py"
      provides: "Wave-0 scaffolds: resume offset, 206-vs-200, md5 reject/accept, atomic finalize, disk precheck"
      contains: "skipif"
    - path: "tests/test_catalog.py"
      provides: "Wave-0 scaffolds: manifest parse, curated subset, footprint/URL, group-by-language"
      contains: "skipif"
    - path: "tests/test_install_state.py"
      provides: "Wave-0 scaffolds: Piper/Kokoro cheap probe + footprint"
      contains: "skipif"
    - path: "tests/test_voice_import.py"
      provides: "Wave-0 scaffolds: filename/traversal validation + pair check"
      contains: "skipif"
    - path: "tests/test_voice_labels.py"
      provides: "Wave-0 scaffolds: override merge + tag search feeding filters (D-14)"
      contains: "skipif"
    - path: "tests/test_uninstall.py"
      provides: "Wave-0 scaffolds: in-use block (D-17), delete pair, bulk .part cleanup (D-18)"
      contains: "skipif"
    - path: "pyproject.toml"
      provides: "network pytest marker registration"
      contains: "network"
  key_links:
    - from: "tests/test_downloader.py"
      to: "diana.downloads.downloader"
      via: "guarded import probe (download_file, has_space)"
      pattern: "from diana.downloads.downloader import"
    - from: "tests/test_catalog.py"
      to: "diana.tts.catalog"
      via: "guarded import probe (parse_manifest)"
      pattern: "diana.tts.catalog"
---

<objective>
Lay the Wave-0 validation foundation for Phase 4 per the Nyquist contract in 04-VALIDATION.md: create the seven new test files as guarded-import + `skipif` scaffolds (collection stays GREEN now, each flips to a live regression gate as its symbol lands), commit the manifest fixture, register the `network` pytest marker, and extend `TTSVoice` with the trailing-defaulted `tags` field that the D-14 custom-label layer (Plan 05) will populate.

Purpose: Every later Phase-4 task gets an `<automated>` verify command that already exists (the scaffold), so the executor never writes a test against an unwritten symbol. The `tags` field is the one shared contract that must exist before the label layer and cross-engine browser are built.

Output: 7 new test files, 1 fixture, the `tags` field on `TTSVoice`, and the `network` marker — full suite collects GREEN with the existing baseline still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engine-management-voice-catalog/04-VALIDATION.md
@.planning/phases/04-engine-management-voice-catalog/04-PATTERNS.md

<interfaces>
<!-- Contracts the scaffolds probe for. Extracted from RESEARCH + codebase; use these exact module homes and signatures so the scaffolds bind when the symbols land. -->

TTSVoice today (diana/tts/base.py) — a plain dataclass; Plan 02/03 added tier/bilingual as trailing defaults:
```
@dataclass
class TTSVoice:
    id: str
    name: str
    language: str
    gender: str
    tier: str = "standard"
    bilingual: bool = False
    # THIS PLAN ADDS: tags: tuple[str, ...] = ()
```

Symbols the scaffolds probe (module home is the implementer's choice in later plans — probe each candidate so the scaffold binds wherever it lands):
- diana.downloads.downloader: download_file(url, dest, expected_md5=None, expected_size=None, progress=None, cancel=None) -> None ; has_space(target, needed_bytes, margin=1.10) -> tuple[bool, int]
- diana.tts.catalog: parse_manifest(json_text_or_dict) -> list[TTSVoice] ; load_bundled_manifest() ; refresh_catalog()
- diana.tts.install_state OR diana.tts.registry: piper_voice_installed(voice_id) -> bool ; piper_footprint_bytes(voice_id) -> int ; kokoro_model_installed() -> bool
- diana.tts.catalog OR a UI helper: safe_voice_dest(uploaded_name) -> Path (the import-filename validator)
- diana.tts.voice_labels OR diana.tts.registry: get_label_overrides(db_path, engine, voice_id) -> dict ; set_label_overrides(...) ; apply_overrides(voice, overrides) -> TTSVoice
- uninstall/cleanup helpers (module home implementer's choice): an in-use-block predicate + a bulk *.part cleanup

Existing scaffold template to mirror EXACTLY: tests/test_native_voices_macos.py and tests/test_native_os_engine.py (guarded try/except import -> `_X_AVAILABLE` flag -> `@pytest.mark.skipif(not _X_AVAILABLE, reason="… implemented in Plan NN")` with a REAL assertion body, never `pass`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the tags field to TTSVoice and register the network marker</name>
  <read_first>
    - diana/tts/base.py (the file being modified — TTSVoice dataclass, currently ends at bilingual: bool = False)
    - diana/tts/piper_engine.py (lines 23-29 — positional VOICES lists that MUST stay valid: 4 positional args; the new field must be trailing-defaulted)
    - diana/tts/kokoro_engine.py (lines 14-24 — same positional VOICES constraint)
    - pyproject.toml (lines 47-50 — [tool.pytest.ini_options]; add the markers key here)
    - 04-PATTERNS.md "Storage shape" note + 04-RESEARCH.md Open Question 2 (A3: extend TTSVoice with tags vs parallel dict — DECISION: extend, mirroring the Phase-3 tier/bilingual trailing-default precedent)
  </read_first>
  <action>
    In diana/tts/base.py, add `tags: tuple[str, ...] = ()` as the LAST field of the TTSVoice dataclass (after `bilingual: bool = False`), with a one-line comment that it holds custom free-text tags surfaced to filter/search (D-14, populated by Plan 05's voice_labels layer). It MUST be trailing-defaulted so the 4-arg positional VOICES lists in piper_engine.py and kokoro_engine.py stay valid with zero edits — this mirrors exactly how Phase 3 added tier/bilingual.
    In pyproject.toml under `[tool.pytest.ini_options]`, add `markers = ["network: hits real network endpoints (HF/GitHub); excluded by default"]` so the Plan-02 opt-in download test does not emit a PytestUnknownMarkWarning.
    Do NOT change any VOICES list, any TTSVoice construction site, or any engine.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q 2>&1 | tail -5 && .venv/bin/python -c "from diana.tts.base import TTSVoice; v=TTSVoice('i','n','l','g'); assert v.tags==() and isinstance(v.tags,tuple); print('tags OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `TTSVoice('i','n','l','g').tags == ()` and is a tuple (default present, positional 4-arg construction still works)
    - `grep -n 'markers' pyproject.toml` shows the `network` marker registered
    - Full suite still passes at the prior baseline (379 passed / 2 skipped) — no regression from the field addition
  </acceptance_criteria>
  <done>tags field added as trailing default; network marker registered; existing suite green and TTSVoice positional construction unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the manifest fixture and the download/catalog/install-state scaffolds</name>
  <read_first>
    - tests/test_native_voices_macos.py (THE template: guarded import -> _AVAILABLE flag -> skipif with real assertion body; fixture-driven pure-parse test reading tests/fixtures/say_voices.txt)
    - tests/test_native_os_engine.py (lines 28-66 — the multi-candidate module-home probe loop pattern for planner's-choice symbols)
    - tests/test_piper_engine.py (tmp_path fixture + patch.dict sys.modules SDK-mock idiom)
    - 04-RESEARCH.md lines 452-501 (verified manifest schema for the fixture: key/name/language{}/quality/num_speakers/speaker_id_map/files{path:{size_bytes,md5_digest}}/aliases; the multi-speaker speaker_id_map example; the raw-download URL pattern)
    - 04-VALIDATION.md "Per-Task Verification Map" + "Wave 0 Requirements" (the exact test names: test_parse_manifest_entry, test_curated_subset, test_resume_offset, test_md5_mismatch_rejects, test_atomic_finalize, test_disk_precheck, and the install_state probes)
  </read_first>
  <action>
    Create `tests/fixtures/voices_manifest.json` as a small valid excerpt of the rhasspy/piper-voices manifest (2-3 entries): include `en_US-lessac-medium` (single-speaker, copy the verified schema and md5/size_bytes from 04-RESEARCH.md:452-481), one British voice (`en_GB-alan-medium`), and one MULTI-speaker voice with a populated `speaker_id_map` (mirror the de_DE-thorsten_emotional example at 04-RESEARCH.md:482-485). Each entry must carry the real `files` map with `.onnx` and `.onnx.json` paths, `size_bytes`, and `md5_digest`.
    Create three scaffold test files mirroring tests/test_native_voices_macos.py's guarded-import + skipif structure (real assertion bodies, never `pass`), using the multi-candidate module-home probe loop from test_native_os_engine.py:28-66 for any planner's-choice symbol:
    - `tests/test_downloader.py` — probe `diana.downloads.downloader` for `download_file`/`has_space`. Tests (skipif-gated): `test_resume_offset` (a `.part` of N bytes -> a resumed request offsets by N; 206 appends, 200 resets to wb), `test_md5_mismatch_rejects` (corrupt content -> `.part` deleted, raises ValueError, no final file), `test_atomic_finalize` (matching md5 -> final file exists via os.replace, `.part` gone), `test_disk_precheck` (monkeypatch `shutil.disk_usage` to return free < needed*margin -> `has_space` returns False). Use tmp_path; stub `requests` with a fake streaming response (mirror test_piper_engine.py's mock idiom) so no real network is hit.
    - `tests/test_downloader_net.py` — a single `@pytest.mark.network` opt-in test that really downloads the 4885-byte `en_US-lessac-medium.onnx.json` from HF (URL in 04-RESEARCH.md:491), interrupts/resumes, and asserts the md5 matches `c1f2b7bddefe113f3255ff9ef234cfd3`. Gate the symbol import too so collection stays green pre-implementation.
    - `tests/test_catalog.py` — probe `diana.tts.catalog` for `parse_manifest`. `test_parse_manifest_entry` (feed the fixture -> first entry maps to a TTSVoice with id=key, language `en-us`, tier from quality, and a footprint == sum of `.onnx`+`.onnx.json` size_bytes, and the built download URL starts with `https://huggingface.co/rhasspy/piper-voices/resolve/main/`), `test_curated_subset` (a curated selector returns a small flat subset; a group-by-language helper returns dict keyed by language for show-all).
    - `tests/test_install_state.py` — probe `diana.tts.install_state` then `diana.tts.registry` for `piper_voice_installed`/`kokoro_model_installed`/`piper_footprint_bytes`. Tests monkeypatch `diana.paths.model_dir` to tmp_path, touch fake `{id}.onnx` / `kokoro-v1.0.onnx`+`voices-v1.0.bin`, and assert true/false probes + footprint.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_downloader.py tests/test_catalog.py tests/test_install_state.py -q 2>&1 | tail -8 && .venv/bin/python -c "import json; d=json.load(open('tests/fixtures/voices_manifest.json')); assert any(e.get('speaker_id_map') for e in d.values()); print('multi-speaker entry present')"</automated>
  </verify>
  <acceptance_criteria>
    - All three files import cleanly; suite collects GREEN (symbol-dependent tests SKIPPED with a "implemented in Plan NN" reason, never errored)
    - `tests/fixtures/voices_manifest.json` is valid JSON with >=2 entries and at least one populated `speaker_id_map`
    - Each scaffold uses guarded import + `@pytest.mark.skipif` with a real assertion body (grep shows `skipif` in each file; grep shows no bare `def test_...(): pass`)
    - `tests/test_downloader_net.py` carries `@pytest.mark.network` and does not run under the default selection
  </acceptance_criteria>
  <done>Fixture + three substrate scaffolds committed; collection green; tests skip-gated and ready to flip live in Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the import / labels / uninstall scaffolds</name>
  <read_first>
    - tests/test_native_os_engine.py (lines 28-66 probe-loop; line ~250 `patch("diana.database.get_setting", return_value=...)` DB-mock idiom)
    - tests/test_database.py (in-memory / tmp_path sqlite setup for the uninstall in-use-block test)
    - diana/dashboard/pages/1_Upload.py (lines 268-284 — the os.path.basename + resolved-prefix traversal guard the import validator reuses; the scaffold asserts that contract)
    - diana/database.py (lines 148-167 get_setting/set_setting; line 222 update_job_status, 248-271 delete_job unlink idiom; the `jobs.tts_voice` column the in-use block reads)
    - 04-RESEARCH.md Pattern 5 (safe_voice_dest reject `../`/absolute/non-.onnx), "Storage shape" (D-14 JSON-valued app_settings key `voice.labels.<engine>.<id>`), Pitfall 5 (D-17 in-use block reads job.tts_voice + `tts.default_voice.<engine>` keys)
  </read_first>
  <action>
    Create three more skipif scaffolds (same template, real assertion bodies, multi-candidate module-home probe):
    - `tests/test_voice_import.py` — probe for `safe_voice_dest` (candidate homes: `diana.tts.catalog`, a UI helper module, or `diana.tts.install_state`). `test_safe_dest`: a plain `en_US-amy-medium.onnx` resolves under `paths.model_dir()`; `../../etc/evil.onnx`, an absolute path, and a `evil.txt` non-`.onnx`/`.onnx.json` name each raise ValueError (HARD-03 traversal + extension allow-list). Monkeypatch `paths.model_dir` to tmp_path.
    - `tests/test_voice_labels.py` — probe for `get_label_overrides`/`set_label_overrides`/`apply_overrides` (candidate homes `diana.tts.voice_labels`, `diana.tts.registry`). Mock `diana.database.get_setting`/`set_setting` (mirror test_native_os_engine.py:~250). Assert: setting an override JSON then reading it round-trips; `apply_overrides(voice, {"language":"fr-fr","tier":"enhanced"})` returns a TTSVoice the existing `filter_voices` honors (filter by language fr-fr finds it); a custom tag in `tags` is found by a plain-substring tag search (NO regex compiled — ReDoS guard, T-04-REDOS).
    - `tests/test_uninstall.py` — probe for the in-use-block predicate + bulk `.part` cleanup (module home implementer's choice). Use a tmp_path model_dir + an in-memory/tmp sqlite db (test_database.py pattern). `test_in_use_block`: a voice id equal to a non-terminal (PENDING) job's `tts_voice`, OR equal to a stored `tts.default_voice.<engine>` value, is REFUSED (D-17); an unreferenced installed voice is allowed and its `.onnx`(+`.onnx.json`) are deleted. `test_clean_partials`: several `*.part` files in model_dir are all removed by the bulk cleanup, while non-`.part` files survive (D-18).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_voice_import.py tests/test_voice_labels.py tests/test_uninstall.py -q 2>&1 | tail -8 && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All three files import cleanly; their symbol-dependent tests SKIP (not error) with an "implemented in Plan NN" reason
    - The traversal test asserts BOTH the basename guard and the resolved-prefix `startswith(model_dir)` check, plus the `.onnx`/`.onnx.json` extension allow-list
    - The labels test proves the merged result stays a `TTSVoice` the Phase-3 `filter_voices` honors, and tag search is plain-substring (no `re.compile` of user input)
    - The uninstall test exercises both the job-reference and default-key arms of the D-17 in-use block, plus the D-18 bulk `.part` glob
    - Full suite collects and runs GREEN end to end (baseline 379 passed; new tests skipped)
  </acceptance_criteria>
  <done>All seven Wave-0 scaffolds committed and skip-gated; the full Nyquist contract from 04-VALIDATION.md is in place; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> filesystem | Scaffolds write fake `.onnx`/`.part` files into pytest `tmp_path` only — never the real per-user cache |
| (no network/UI here) | This plan adds tests + a dataclass field + a marker; the live network/upload boundaries belong to Plans 02-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | scaffold test files writing outside tmp_path | mitigate | All file I/O in scaffolds is confined to pytest `tmp_path`; `diana.paths.model_dir` is monkeypatched, never the real dir |
| T-04-02 | Denial of Service | `test_downloader_net.py` accidentally hitting the network in CI | mitigate | `@pytest.mark.network` registered this plan + excluded by default; only opt-in `-m network` runs it |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all deps pre-existing and pinned (RESEARCH Package Legitimacy Audit: requests/streamlit/piper-tts/kokoro-onnx all [OK]); no `[ASSUMED]`/`[SUS]` packages introduced |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -q` collects and runs GREEN at the existing baseline (379 passed) with the new files SKIPPED, not errored.
- The seven new scaffolds + fixture exist; each scaffold contains `skipif` and a real assertion body.
- `TTSVoice` exposes `tags` (trailing default); the `network` marker is registered.
</verification>

<success_criteria>
- All Wave-0 gaps from 04-VALIDATION.md "Wave 0 Requirements" are filled (7 test files + fixture + marker).
- The full suite is green; no symbol-dependent test errors at collection.
- `TTSVoice.tags` exists as a trailing-defaulted tuple, unblocking the D-14 label layer (Plan 05) and the cross-engine browser (Plan 06).
</success_criteria>

<output>
Create `.planning/phases/04-engine-management-voice-catalog/04-01-SUMMARY.md` when done.
</output>
