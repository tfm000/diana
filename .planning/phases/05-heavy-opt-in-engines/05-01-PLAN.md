---
phase: 05-heavy-opt-in-engines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_heavy_install.py
  - tests/test_gpu_probe.py
  - tests/test_install_state_heavy.py
  - tests/test_license_gate.py
  - tests/test_orpheus_engine.py
  - tests/test_f5_engine.py
  - tests/test_fish_engine.py
  - tests/test_registry_heavy.py
  - tests/test_custom_voices.py
  - tests/test_heavy_failfast.py
autonomous: true
requirements: [HEAVY-01, HEAVY-02, HEAVY-03]
user_setup: []

must_haves:
  truths:
    - "All 11 Wave-0 scaffold files collect and the full suite stays green with new skips — the Nyquist contract for HEAVY-01/02/03 (RESEARCH Validation Architecture)"
    - "Each scaffold flips skip->pass automatically when its symbol lands in Waves 2-7, with NO later test-file edits (the Phase-3/4 scaffold discipline)"
    - "Scaffolds encode the security invariants up front: no-heavy-import on cheap paths (D-17), text-as-data subprocess, path-safety, accept-once license (D-08), fail-fast (D-16)"
    - "A shared tests/conftest.py provides the fixtures every heavy-engine test needs: fake venv dir, mock subprocess.run/Popen, fake nvidia-smi output, temp clip files, tmp per-user paths"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared fixtures (fake venv, mock subprocess, fake nvidia-smi, temp clip, tmp paths)"
      contains: "def "
    - path: "tests/test_heavy_install.py"
      provides: "RED/skipif scaffold for the uv provisioner + two-phase progress"
    - path: "tests/test_gpu_probe.py"
      provides: "RED/skipif scaffold for the torch-free nvidia-smi gate"
    - path: "tests/test_registry_heavy.py"
      provides: "RED/skipif scaffold asserting heavy engines register with no heavy import"
    - path: "tests/test_orpheus_engine.py"
      provides: "RED/skipif scaffold for Orpheus voices + subprocess synth"
    - path: "tests/test_custom_voices.py"
      provides: "RED/skipif scaffold for clip validation + path safety"
  key_links:
    - from: "tests/test_heavy_install.py"
      to: "diana.tts.heavy_install"
      via: "import-probe with skipif until the symbol lands (Wave 3)"
      pattern: "importlib|pytest.mark.skipif|pytest.importorskip"
    - from: "tests/test_registry_heavy.py"
      to: "diana.tts.registry.list_engines"
      via: "skip until 'orpheus' in list_engines() (Wave 2)"
      pattern: "list_engines"
---

<objective>
Land the Phase-5 Nyquist Wave-0 test scaffold: a shared `tests/conftest.py` plus ten RED/skipif test files covering every heavy-engine symbol that lands in Waves 2-7. This is the test-first contract — collection stays green NOW (unlanded symbols skip), and each test auto-flips to a live regression gate the moment its symbol is implemented, with no later edits to the test files.

This mirrors Diana's proven Phase-3 (`03-01`) and Phase-4 (`04-01`) scaffold pattern exactly: import-guard future symbols, skipif-gate the dependent tests, give every test a REAL assertion body (never `pass`/`xfail`), and probe multiple candidate module homes for planner's-choice symbols.

Purpose: `nyquist_validation` is enabled (`.planning/config.json`). Heavy engines cannot run torch/llama in CI, so these tests MOCK the subprocess/venv layer; real synthesis is manual UAT (per RESEARCH Validation Architecture). The scaffold makes every later wave verifiable without writing new test files mid-stream.
Output: 11 files (1 shared conftest + 10 test scaffolds); full suite stays green (current 461 pass + new skips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-heavy-opt-in-engines/05-RESEARCH.md
@.planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md

<interfaces>
<!-- Test mock idioms to copy (PATTERNS.md Metadata + existing tests). Executor uses these directly. -->

Existing analog tests to mirror:
- tests/test_native_os_engine.py — subprocess mock: patch("diana.tts.native_os_engine.subprocess.run", side_effect=_fake_run) returning MagicMock(returncode=0, stderr=""); absent/fake SDK via patch.dict(sys.modules, {...}) with async fakes.
- tests/test_install_state.py — filesystem probe: monkeypatch.setattr("diana.paths.model_dir", lambda: tmp_path) then touch fake files.
- tests/test_tts_registry.py — registry assertions over _ASCII_ONLY_ENGINES, list_engines(), dynamic voices.

pytest config already present (pyproject.toml [tool.pytest.ini_options]):
- asyncio_mode = "auto" (no @pytest.mark.asyncio needed)
- testpaths = ["tests"], addopts = "-m 'not network'", marker: network

Planner's-choice symbol homes the scaffolds must import-probe (multi-candidate, tolerant):
- heavy_install: diana.tts.heavy_install.provision_venv / install_engine ; license_accepted / accept_license (candidate home: diana.tts.heavy_install OR diana.tts.install_state)
- gpu_probe: diana.tts.gpu_probe.capable_nvidia_gpu ; FISH_MIN_VRAM_GB
- install_state: diana.tts.install_state.heavy_engine_installed / heavy_footprint_bytes / uninstall_heavy_engine
- registry: diana.tts.registry.list_engines (includes orpheus/f5/fish) ; _ASCII_ONLY_ENGINES ; heavy_engine_failfast
- engines: diana.tts.orpheus_engine.OrpheusEngine ; diana.tts.f5_engine.F5Engine ; diana.tts.fish_engine.FishEngine
- custom_voices: diana.tts.custom_voices.validate_clip / safe_custom_voice_dest / list_custom_voices / save_custom_voice / remove_custom_voice
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest fixtures + install/provision/license scaffolds</name>
  <files>tests/conftest.py, tests/test_heavy_install.py, tests/test_gpu_probe.py, tests/test_install_state_heavy.py, tests/test_license_gate.py</files>
  <read_first>
    - tests/test_install_state.py (the monkeypatch paths + touch-fake-file idiom to extend with venvs_dir/hf_cache_dir)
    - tests/test_native_os_engine.py (subprocess mock idiom: patch subprocess.run/Popen returning MagicMock)
    - .planning/phases/05-heavy-opt-in-engines/05-RESEARCH.md ("Validation Architecture" → "Wave 0 Gaps" + "Phase Requirements → Test Map" + Pattern 1/3/4 for the exact behaviors)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md ("Metadata" → test mock idioms; install_state / gpu_probe sections)
    - diana/tts/install_state.py (the cheap-probe contract these tests assert)
    - diana/downloads/downloader.py (has_space signature the install gate uses)
  </read_first>
  <action>
Create `tests/conftest.py` with shared fixtures: `tmp_data_paths` (monkeypatch `diana.paths.venvs_dir`/`hf_cache_dir`/`custom_voices_dir`/`model_dir` to subfolders of `tmp_path` + create them); `fake_venv` (build a venvs_dir/<name>/bin/python placeholder file + the `.{engine}.installed` marker so install-state probes resolve True); `mock_uv` (patch `subprocess.Popen` to a fake streaming a few stdout lines then returncode 0; patch `subprocess.run` to MagicMock(returncode=0, stderr="")); `fake_nvidia_smi` (patch `shutil.which` to a path and `subprocess.run` to return a memory.total CSV string, parametrizable to absent/below-floor/above-floor); `temp_clip` (write a short real WAV via `soundfile.write` at a chosen duration/samplerate + a sibling `.txt` transcript).

Create the four scaffolds, each import-probing its planner's-choice home with `pytest.importorskip`/a tolerant try-import + `pytest.mark.skipif`, and each with a REAL assertion body (never `pass`/`xfail`):
- `test_heavy_install.py`: `provision_venv` builds the correct `uv` argv ORDER per OS — `uv venv --python 3.12 <venv>` then `uv pip install --python <venv-python> <pkgs> [--extra-index-url ...]`, using `paths.uv_binary()` not PATH; two-phase progress streams a uv stdout line into `state["step"]` and weight bytes into `state["downloaded"]`; `install_engine` runs `has_space` BEFORE any byte and writes the `.{engine}.installed` marker on success. Mock all subprocess.
- `test_gpu_probe.py`: `capable_nvidia_gpu()` parses `memory.total` and returns `(True, vram_gb, "")` at/above `FISH_MIN_VRAM_GB` (12), `(False, 0, reason)` when `shutil.which("nvidia-smi")` is None or VRAM below floor; assert `"torch"` not in `sys.modules` after the call (D-09 torch-free).
- `test_install_state_heavy.py`: `heavy_engine_installed(engine)` True iff the venv python + marker exist (filesystem only); `heavy_footprint_bytes` sums sizes; `uninstall_heavy_engine` scoped to `venvs_dir()`; assert no `torch`/`llama_cpp`/`orpheus_cpp`/`f5_tts` in `sys.modules` after the cheap call (D-17/ENGINE-01).
- `test_license_gate.py`: `accept_license(db, engine)` then `license_accepted(db, engine)` returns True and survives a re-read (accept-once, D-08) over a temp DB; an un-accepted engine returns False. Probe candidate homes heavy_install OR install_state.
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/test_heavy_install.py tests/test_gpu_probe.py tests/test_install_state_heavy.py tests/test_license_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All four files collect with zero collection errors: `/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/test_heavy_install.py tests/test_gpu_probe.py tests/test_install_state_heavy.py tests/test_license_gate.py --collect-only -q` exits 0
    - The four files report SKIPPED (not failed, not errored) because their symbols are unlanded: the run shows skips and 0 failures
    - `tests/conftest.py` defines fixtures named `tmp_data_paths`, `fake_venv`, `mock_uv`, `fake_nvidia_smi`, `temp_clip` (grep each `def ` / fixture name present)
    - No test body is a bare `pass` or uses `xfail`: `grep -nE "pytest.mark.xfail|^\s*pass\s*$" tests/test_heavy_install.py tests/test_gpu_probe.py tests/test_install_state_heavy.py tests/test_license_gate.py` returns nothing
    - Each scaffold references `paths.uv_binary` / `capable_nvidia_gpu` / `heavy_engine_installed` / `license_accepted` (the symbols it gates on)
  </acceptance_criteria>
  <done>The provisioner/GPU/install-state/license scaffolds + shared conftest exist, collect cleanly, skip pending their symbols, and carry real assertion bodies.</done>
</task>

<task type="auto">
  <name>Task 2: engine + registry scaffolds</name>
  <files>tests/test_orpheus_engine.py, tests/test_f5_engine.py, tests/test_fish_engine.py, tests/test_registry_heavy.py</files>
  <read_first>
    - tests/test_native_os_engine.py (subprocess synth mock + patch.dict(sys.modules) fake-SDK injection — the exact idiom these copy)
    - tests/test_tts_registry.py (assertions over list_engines()/_ASCII_ONLY_ENGINES/dynamic voices)
    - diana/tts/base.py (TTSEngine Protocol + TTSVoice fields the engines must satisfy)
    - diana/tts/native_os_engine.py (lines 199-233 — the run_in_executor + subprocess.run + temp-WAV-unlink shape the heavy engines mirror)
    - diana/tts/registry.py (lines 14-31, 56-82, 194-207 — the seams the heavy branches extend)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md (orpheus_engine / f5_engine / fish_engine / registry sections)
  </read_first>
  <action>
Create four scaffolds, each import-probing its module with skipif + real assertions:
- `test_orpheus_engine.py`: `OrpheusEngine.VOICES` is a static list of 8 named `TTSVoice` enumerable with NO heavy import (assert `orpheus_cpp` not in `sys.modules`); `initialize()` raises `FileNotFoundError` naming "Settings ▸ Voices" when `heavy_engine_installed("orpheus")` is False (monkeypatch it False); `synthesize` builds the subprocess cmd `[<venv-python>, <orpheus_worker.py>]` with the text passed as stdin JSON (`{"text","voice_id","out"}`), `HF_HOME` set in env, and returns the bytes the mock writes to the temp WAV. Mock subprocess.run.
- `test_f5_engine.py`: `F5Engine.list_voices()` includes the bundled default voice (and any custom voices when present); `synthesize` builds a cmd carrying `ref_file`/`ref_text`/`gen_text` JSON into the torch venv; enumeration imports no torch (`sys.modules` assert).
- `test_fish_engine.py`: `FishEngine.initialize()` raises an actionable error when NOT (installed AND `capable_nvidia_gpu()` ok) — monkeypatch both; subprocess cmd shape mirrors F5.
- `test_registry_heavy.py`: `list_engines()` includes `"orpheus"`, `"f5"`, `"fish"`; `_ASCII_ONLY_ENGINES` maps all three to False and `engine_is_ascii_only("orpheus")` is False; a cheap-path call (`list_engines()` + `engine_is_ascii_only`) leaves `sys.modules` free of `torch`/`llama_cpp`/`orpheus_cpp`/`f5_tts` (D-17/ENGINE-01). Gate the engine-name assertions on `"orpheus" in list_engines()` so they skip until Wave 2.
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/test_orpheus_engine.py tests/test_f5_engine.py tests/test_fish_engine.py tests/test_registry_heavy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All four files collect with zero collection errors (`--collect-only -q` exits 0)
    - The files report SKIPPED (symbols unlanded), 0 failures
    - `test_registry_heavy.py` asserts absence of heavy modules from `sys.modules` (grep `sys.modules` present)
    - `test_orpheus_engine.py` asserts 8 voices and stdin-JSON text passing (grep `VOICES` and `stdin`/`input=` present)
    - No `pass`/`xfail` bodies (grep returns nothing as in Task 1)
  </acceptance_criteria>
  <done>Orpheus/F5/Fish engine scaffolds + the registry no-heavy-import scaffold exist, collect, skip pending their symbols, with real assertions.</done>
</task>

<task type="auto">
  <name>Task 3: custom-voices + fail-fast scaffolds</name>
  <files>tests/test_custom_voices.py, tests/test_heavy_failfast.py</files>
  <read_first>
    - diana/tts/catalog.py (lines 159-188 — safe_voice_dest: basename + ext allow-list + containment, the path-safety template these tests assert)
    - diana/tts/voice_labels.py (lines 46-82 — app_settings JSON metadata + malformed-JSON tolerance the custom-voice metadata mirrors)
    - diana/dashboard/pages/5_Settings.py (lines 475-517 — the (ok,msg) import-rejection pattern validate_clip mirrors)
    - diana/dashboard/pages/1_Upload.py (lines 43-82, 364-388 — _engine_readiness + the currently-ungated Convert button the fail-fast helper feeds)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md (custom_voices section; "Engine readiness → actionable message")
  </read_first>
  <action>
Create two scaffolds with skipif + real assertions:
- `test_custom_voices.py`: `validate_clip(audio_path, transcript)` returns `(True, msg)` for a ~2-12 s clip with a non-empty transcript and `(False, msg)` for a <~1 s clip, an empty/whitespace transcript, and a disallowed format — and NEVER raises (use the `temp_clip` fixture at several durations; assert sub-24 kHz like 16 kHz is accepted, Pitfall 5/7); `safe_custom_voice_dest(name)` strips path components, enforces a `.wav`/`.mp3`/`.txt` allow-list, and raises `ValueError` on traversal or a disallowed extension (mirror the safe_voice_dest assertions); `save_custom_voice`/`list_custom_voices`/`remove_custom_voice` round-trip metadata through app_settings on a temp DB, and a malformed stored value degrades to an empty result rather than raising (T-04-LBLJSON analog).
- `test_heavy_failfast.py`: `registry.heavy_engine_failfast(engine_name)` returns an actionable string naming "Settings ▸ Voices" when a heavy engine is selected but `heavy_engine_installed` is False (monkeypatch False), and returns `None` when installed or for a non-heavy engine (native_os/kokoro/piper) — the D-16 "refuse before the job, never mid-job" contract. Probe the symbol home (registry) with skipif.
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/test_custom_voices.py tests/test_heavy_failfast.py -q && /Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Both files collect with zero collection errors and report SKIPPED, 0 failures
    - `test_custom_voices.py` asserts validate_clip returns a tuple and never raises (grep `validate_clip` + a `(False,` or `, msg` assertion present), and asserts a traversal `ValueError`
    - FULL suite stays green: `/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q` shows the prior 461 passed plus the new skips, 0 failures, 0 collection errors
    - No `pass`/`xfail` bodies
  </acceptance_criteria>
  <done>Custom-voice validation/path-safety/metadata and the fail-fast scaffolds exist; the entire suite collects and stays green with new skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → future heavy modules | Tests import-probe modules that do not exist yet; they must skip cleanly, never error collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-IMP | DoS (perf) | cheap-path enumeration/badge/probe | mitigate | Scaffolds assert `torch`/`llama_cpp`/`orpheus_cpp`/`f5_tts` absent from `sys.modules` after cheap calls (Task 2/Task 1) — encodes ENGINE-01/D-17 as a test gate |
| T-05-CMD | Tampering | subprocess synth (text→shell) | mitigate | Engine scaffolds assert text is passed as stdin JSON / list argv, never a shell string (Task 2) |
| T-05-PATH | Tampering | custom-voice upload filename | mitigate | `test_custom_voices` asserts safe_custom_voice_dest raises on traversal + enforces an extension allow-list (Task 3) |
| T-05-SC | Tampering/Elevation | scaffold itself installs nothing | accept | This plan creates only test files; no package install or network — no supply-chain surface here. Real supply-chain gating is modeled in 05-03 |
</threat_model>

<verification>
- `/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q` is green: prior 461 passed + the new heavy scaffolds as SKIPPED, 0 failures, 0 collection errors.
- `/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ --collect-only -q` exits 0 (no import/collection error from the unlanded-symbol probes).
- No scaffold uses `pass`/`xfail` as a test body.
</verification>

<success_criteria>
- 11 files created (conftest + 10 scaffolds); each collects and skips pending its symbol.
- Every later wave (2-7) can flip its scaffold(s) green by landing the symbol, with no edits to these files.
- The security invariants (no-heavy-import, text-as-data, path-safety, accept-once license, fail-fast) are encoded as assertions now.
</success_criteria>

<output>
Create `.planning/phases/05-heavy-opt-in-engines/05-01-SUMMARY.md` when done.
</output>
