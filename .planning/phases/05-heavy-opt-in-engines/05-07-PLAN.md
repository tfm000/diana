---
phase: 05-heavy-opt-in-engines
plan: 07
type: execute
wave: 7
depends_on: ["05-01", "05-02", "05-03", "05-05", "05-06"]
files_modified:
  - diana/tts/fish_engine.py
  - diana/tts/heavy_workers/fish_worker.py
  - diana/tts/registry.py
  - diana/dashboard/pages/1_Upload.py
  - diana/dashboard/pages/5_Settings.py
autonomous: false
requirements: [HEAVY-03]
user_setup:
  - service: huggingface
    why: "Fish S2 Pro downloads the fishaudio/s2-pro weights (non-commercial license) from a public HF repo on install (NVIDIA GPU machines only)"
    env_vars: []

must_haves:
  truths:
    - "Fish Audio S2 Pro is installable + usable as an opt-in heavy engine, completing the three-engine lineup this phase (HEAVY-03, D-01)"
    - "Fish is SHOWN BUT DISABLED with a 'requires a capable GPU (~12+ GB VRAM)' reason when no capable NVIDIA GPU is detected — not hidden (D-10, the reconciled HEAVY-03/SC#3 wording)"
    - "Before any Fish download, the accept-once non-commercial license disclosure (Fish Audio Research License / CC-BY-NC-SA-4.0) is shown and must be accepted; acceptance persists (D-08)"
    - "Fish implements the TTSEngine Protocol with lazy SDK imports — no torch on the cheap path; its initialize() gates on BOTH installed AND a capable GPU, and synthesis runs out-of-process (D-17)"
    - "Choosing Fish when not installed (or without a capable GPU) disables Convert with an actionable prompt — never errors mid-job (D-16); Fish reuses the engine-agnostic Custom Voices library + a bundled default (A6)"
  artifacts:
    - path: "diana/tts/fish_engine.py"
      provides: "FishEngine (TTSEngine, GPU+install gated, subprocess synth in the torch venv) + fish_install_spec"
      contains: "class FishEngine"
      min_lines: 55
    - path: "diana/tts/heavy_workers/fish_worker.py"
      provides: "venv-run worker: stdin JSON -> fish-speech inference -> WAV; --prefetch mode"
      contains: "fish"
    - path: "diana/tts/registry.py"
      provides: "fish across list_engines/_get_engine_class/create_engine/get_engine_voices"
  key_links:
    - from: "diana/dashboard/pages/5_Settings.py (Fish row)"
      to: "diana/tts/gpu_probe.py::capable_nvidia_gpu"
      via: "shown-but-disabled-with-reason when no capable GPU (D-10)"
      pattern: "capable_nvidia_gpu"
    - from: "diana/tts/fish_engine.py::initialize"
      to: "gpu_probe.capable_nvidia_gpu + install_state.heavy_engine_installed"
      via: "gate on BOTH before synth (D-10/D-16)"
      pattern: "capable_nvidia_gpu|heavy_engine_installed"
    - from: "diana/dashboard/pages/5_Settings.py (Fish row)"
      to: "diana/tts/heavy_install.py::license_accepted / accept_license"
      via: "accept-once NC-license gate before any download (D-08)"
      pattern: "license_accepted|accept_license"
---

<objective>
## Phase Goal

**As a** power user with a capable GPU, **I want to** opt into Fish Audio S2 Pro after accepting its non-commercial license, **so that** I get top-tier neural cloning — while users without a capable GPU clearly see why it is unavailable rather than it being hidden.

This final slice completes the three-engine lineup (D-01). Fish is NVIDIA-CUDA-focused (~12+ GB VRAM, effectively not Apple-MPS — RESEARCH A5), so it is SHOWN BUT DISABLED with a reason when no capable GPU is detected (D-10 — the reconciled wording from 05-02), reusing the torch-free `gpu_probe` from 05-02. It reuses the F5 license gate (D-08, with Fish's own disclosure), the shared heavy-install substrate, and the engine-agnostic Custom Voices library + a bundled default voice (A6: Fish is zero-shot clone).

torch/fish-speech live ONLY in `heavy_workers/fish_worker.py`. Per RESEARCH Q-B, Fish defaults to the shared `torch` venv (D-03); if F5's torch build conflicts with Fish's CUDA requirement at execution, fall back to a dedicated Fish venv (update the install_state venv mapping) — verified at install time. The fish-speech inference signature (A6/Q-D) is confirmed against the pinned commit during execution.

Purpose: deliver HEAVY-03 and close the phase's three-engine scope.
Output: Fish installable behind the GPU + license gates, selectable, synthesizing; `test_fish_engine` green; UI human-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-heavy-opt-in-engines/05-CONTEXT.md
@.planning/phases/05-heavy-opt-in-engines/05-RESEARCH.md
@.planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md
@.planning/phases/05-heavy-opt-in-engines/05-03-SUMMARY.md

<interfaces>
<!-- Reuse directly. -->

diana/tts/f5_engine.py (05-05/06): the SIBLING to clone — subprocess synth + bundled default + custom-voice resolution
diana/tts/gpu_probe.py (05-02): capable_nvidia_gpu() -> (ok, vram_gb, reason) ; FISH_MIN_VRAM_GB = 12
diana/tts/install_state.py (05-02): heavy_engine_installed("fish") (maps to shared "torch" venv by default — Q-B fallback may add a "fish" venv); uninstall_heavy_engine("fish")
diana/tts/custom_voices.py (05-06): list_custom_voices() ; custom_voice_ref(voice_id)  (ENGINE-AGNOSTIC — Fish reuses them)
diana/tts/heavy_install.py (05-03): HeavyInstallSpec ; install_engine ; license_accepted/accept_license
diana/dashboard/pages/5_Settings.py (05-04/05): _render_heavy_engine_row(engine, spec[, license][, gpu_gate]) ; _cross_engine_badge ; "Heavy opt-in engines" subsection
diana/dashboard/pages/1_Upload.py (05-04): generic D-16 fail-fast (heavy_engine_failfast covers fish) ; _engine_readiness (add fish branch)
diana/data/voices/f5_default.{wav,txt} (05-05): the bundled reference clip Fish reuses as its default

RESEARCH Fish facts (confirm at execution per A5/A6/Q-D, from 05-03 SUMMARY):
  install: uv pip install "fish-speech @ git+https://github.com/fishaudio/fish-speech@<pinned-sha>" into the torch venv (Q-D)
  weights: fishaudio/s2-pro (Fish Audio Research License / CC-BY-NC-SA-4.0, NON-commercial) ; NVIDIA ~12-24 GB VRAM, not Apple-MPS (A5)
  voice shape: zero-shot reference clone (A6) -> reuse Custom Voices + bundled default ; if preset named voices exist, list_voices becomes static (verify at execution)
  license link: https://huggingface.co/fishaudio/s2-pro (model card)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FishEngine + worker (GPU+install gated) + registry/Upload wiring</name>
  <files>diana/tts/fish_engine.py, diana/tts/heavy_workers/fish_worker.py, diana/tts/registry.py, diana/dashboard/pages/1_Upload.py</files>
  <behavior>
    - FishEngine.initialize() raises an actionable error unless BOTH heavy_engine_installed("fish") AND capable_nvidia_gpu() ok
    - list_voices() = bundled default + custom_voices.list_custom_voices(); enumeration imports NO torch
    - synthesize() passes ref_file/ref_text/gen_text JSON to the fish worker in the torch venv (text as data)
    - registry: "fish" in list_engines(); routes to FishEngine; cheap path imports no torch
    - Upload: an _engine_readiness fish branch surfaces install + GPU-gate state; the generic fail-fast already gates Convert
  </behavior>
  <read_first>
    - diana/tts/f5_engine.py (the sibling to clone — class shape, _subprocess_synth, _resolve_ref, install_spec)
    - diana/tts/gpu_probe.py (capable_nvidia_gpu — the gate to call in initialize)
    - diana/tts/custom_voices.py (list_custom_voices / custom_voice_ref — engine-agnostic, reused)
    - diana/tts/registry.py (full — add fish across the seams, mirroring f5)
    - diana/dashboard/pages/1_Upload.py (lines 43-82 _engine_readiness — add fish branch)
    - .planning/phases/05-heavy-opt-in-engines/05-RESEARCH.md (HEAVY-03 row; Pattern 4 GPU gate; Pitfall 4; Q-B venv sharing; Q-D fish install; A5/A6)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md (fish_engine section — "Clone the F5 engine. Delta: initialize() gates on BOTH ...")
    - tests/test_fish_engine.py (the scaffold this flips green)
  </read_first>
  <action>
Create `diana/tts/fish_engine.py` by cloning `F5Engine` (module top: stdlib + `from diana.tts.base import TTSVoice` only — NO torch/fish_speech). `class FishEngine` with `name = "fish"`; `list_voices()` = a bundled default (reuse the `diana/data/voices/f5_default.{wav,txt}` package resource as Fish's default reference — A6) PLUS `custom_voices.list_custom_voices()` (the shared engine-agnostic pool, D-11). `initialize()` gates on BOTH: lazy-import install_state + gpu_probe; raise `FileNotFoundError("Fish S2 Pro not installed — open Settings ▸ Voices and click Install.")` when `not heavy_engine_installed("fish")`, and raise `RuntimeError(reason)` when `capable_nvidia_gpu()` is not ok (D-10/D-16 defence-in-depth). `_resolve_ref(voice)` maps the default id -> the bundled clip + transcript, else `custom_voices.custom_voice_ref(voice)`. `async synthesize(...)` -> `_subprocess_synth` building `[torch-venv python, fish_worker.py]` with `{ref_file, ref_text, gen_text, out, hf_cache}` stdin JSON + `HF_HOME` env (text as data, list argv — T-05-CMD), reading + unlinking the temp WAV. Add `fish_install_spec() -> HeavyInstallSpec` (engine="fish", venv_name="torch" by default — Q-B; `packages` the pinned `fish-speech @ git+...@<sha>` from 05-03 Task 1; the per-OS torch CUDA index where Fish needs it; `prefetch_argv=[heavy_worker("fish_worker.py"), "--prefetch"]`; estimate deps/weights bytes — exact totals read live). Note the Q-B fallback: if installing fish-speech into the shared torch venv conflicts with F5's torch at execution, switch `venv_name` to a dedicated `"fish"` and update the install_state venv mapping (document the deviation).

Create `diana/tts/heavy_workers/fish_worker.py` (run BY the torch venv — imports fish_speech/soundfile freely). `--prefetch`: download the `fishaudio/s2-pro` weights into HF_HOME and exit 0. Else read JSON from stdin and run the fish-speech reference-clone inference (ref audio + ref text + gen text -> waveform), `sf.write(out, wav, sr, format="WAV")`. CONFIRM the exact fish-speech inference call against the pinned commit at execution (A6/Q-D) — the signature is MEDIUM-confidence; mirror the f5_worker shape and adjust to the real fish-speech API. No `__init__.py` in heavy_workers.

In `diana/tts/registry.py`: add `"fish"` to `list_engines()` (after f5); add the lazy `_get_engine_class` + `create_engine` branches (construct `FishEngine()`); add a dynamic `get_engine_voices("fish")` branch = bundled default + `custom_voices.list_custom_voices()` (mirroring `_f5_voices`, import-light, no torch).

In `diana/dashboard/pages/1_Upload.py`: extend `_engine_readiness` with a `fish` branch: `ok_gpu, _, reason = gpu_probe.capable_nvidia_gpu()`; if not `ok_gpu` return `(False, reason)` (the D-10 reason on the badge); else if `heavy_engine_installed("fish")` return `(True, "Ready — Fish S2 Pro installed")` else `(False, "Large download (torch + model), install in Settings ▸ Voices")`. The generic Convert fail-fast already covers fish (no change).
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/test_fish_engine.py -x -q && /Users/tyler/Repos/diana/.venv/bin/python -c "import diana.tts.registry as r, sys; r.get_engine_voices('fish'); assert 'fish' in r.list_engines(); assert 'torch' not in sys.modules"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fish_engine.py` flips skip->PASS (initialize gates on BOTH installed AND GPU; subprocess cmd shape)
    - `grep -nE "import torch|import fish_speech" diana/tts/fish_engine.py` returns nothing
    - `'fish' in registry.list_engines()`; `registry.get_engine_voices('fish')` returns default + custom and leaves `torch` out of `sys.modules`
    - `diana/tts/heavy_workers/fish_worker.py` exists with a `--prefetch` branch and reads ref_file/ref_text/gen_text from stdin JSON; no heavy_workers `__init__.py`
    - `1_Upload.py` `_engine_readiness` has a `fish` branch surfacing the GPU-gate reason when no capable GPU
  </acceptance_criteria>
  <done>FishEngine + worker exist with no torch on the cheap path, gate on install + GPU, reuse the engine-agnostic custom voices, register and route; the scaffold is green.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Fish install row — shown-but-disabled GPU gate (D-10) + NC-license gate (D-08)</name>
  <files>diana/dashboard/pages/5_Settings.py</files>
  <read_first>
    - diana/dashboard/pages/5_Settings.py (_render_heavy_engine_row + license-gate from 05-04/05; lines 619-707 the kokoro row disabled-button idiom for the shown-disabled state; _cross_engine_badge; "Heavy opt-in engines" subsection)
    - diana/tts/gpu_probe.py (capable_nvidia_gpu — the D-10 gate)
    - diana/tts/heavy_install.py (license_accepted/accept_license)
    - diana/tts/fish_engine.py (fish_install_spec — from Task 1)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md (5_Settings.py "GPU gate (D-10)" + "License gate (D-08)")
  </read_first>
  <action>
Extend `_render_heavy_engine_row` (from 05-04/05) to accept an optional `gpu_gate` (the `capable_nvidia_gpu()` result). When a gpu_gate is required and NOT ok, render the row SHOWN BUT DISABLED (D-10): the engine title + a disabled `st.button("Install", disabled=True)` + `st.caption(reason)` (e.g. "requires a capable GPU (~12+ GB VRAM)") — do NOT hide the row and do NOT show the license/footprint controls. When the gpu_gate is ok (or absent), fall through to the existing license gate + footprint-confirm + has_space + Install flow.

Invoke the row for Fish in the "Heavy opt-in engines" subsection: `_render_heavy_engine_row("fish", fish_engine.fish_install_spec(), license={"text": "Fish S2 Pro weights are released under the Fish Audio Research License / CC-BY-NC-SA-4.0 (non-commercial / personal use only).", "url": "https://huggingface.co/fishaudio/s2-pro"}, gpu_gate=gpu_probe.capable_nvidia_gpu())`. The accept-once license flag is `license.accepted.fish` (D-08, reusing the F5 gate mechanism). Add a `fish` branch to `_cross_engine_badge`: when no capable GPU, caption the GPU-required reason; when installed, "Ready · Fish installed"; else "~large, downloads on install" (cheap probe + gpu_probe only, NO torch import).
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -c "import ast; ast.parse(open('diana/dashboard/pages/5_Settings.py').read())" && /Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `5_Settings.py` parses and the FULL suite is green (0 failures) — all Wave-0 scaffolds now landed
    - `grep -n "capable_nvidia_gpu" diana/dashboard/pages/5_Settings.py` present and the Fish row renders a DISABLED Install + reason caption when not ok (shown-but-disabled, D-10 — never hidden)
    - `grep -n "license.accepted.fish\|accept_license" diana/dashboard/pages/5_Settings.py` present; Fish license disclosure names "non-commercial" + links the model card
    - `grep -n "fish_install_spec" diana/dashboard/pages/5_Settings.py` present (no hardcoded Fish repo IDs/SHA in the page)
    - No torch/fish_speech import added: `grep -nE "import torch|import fish_speech" diana/dashboard/pages/5_Settings.py` returns nothing
  </acceptance_criteria>
  <done>The Settings Fish row is shown-but-disabled-with-reason without a capable GPU (D-10), and behind the GPU gate shows the NC-license accept-once + footprint-confirm + install; the badge reflects state — all cheap, no torch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: human-verify the Fish slice (GPU gate, license, install/synth)</name>
  <files>diana/dashboard/pages/5_Settings.py, diana/dashboard/pages/1_Upload.py</files>
  <action>
Pause for human verification AFTER Tasks 1-2 — no new feature code here. Run the agent PRE-CHECK FIRST (AppTest interaction tests + Playwright-MCP screenshots inspected by a vision agent — the active UI policy), fix any defect in fish_engine.py / registry.py / 5_Settings.py / 1_Upload.py and re-run, THEN hand to the user. Most verifying machines are macOS with NO nvidia-smi, so the shown-but-disabled path is the primary automatable check; a real GPU install/synthesize REQUIRES an NVIDIA >=12 GB machine and is almost certainly deferred to .planning/phases/05-heavy-opt-in-engines/05-HUMAN-UAT.md (like the Phase-3 Windows UAT — not a defect). Also confirm the 05-02 D-10 wording reconciliation landed.
  </action>
  <what-built>
FishEngine (GPU+install gated, out-of-process synth in the torch venv, reusing the engine-agnostic Custom Voices + bundled default), the Settings Fish install row that is shown-but-disabled-with-reason without a capable GPU (D-10) and behind the GPU gate shows the NC-license accept-once + footprint confirm + install, the Upload readiness badge, and the generic D-16 fail-fast.
  </what-built>
  <how-to-verify>
PRE-CHECK FIRST (agent, automatable — most of this phase's verifying machines are macOS with NO nvidia-smi, so the shown-disabled path is the primary automatable check):
  1. AppTest: 5_Settings.py Voices tab on a machine without a capable GPU — assert the Fish row is SHOWN with a DISABLED Install button and the "requires a capable GPU (~12+ GB VRAM)" reason, and that NO download can start (D-10). Confirm it is NOT hidden.
  2. AppTest: 1_Upload.py — select Fish; assert Convert is disabled with the fail-fast/GPU reason (D-16/D-10).
  3. AppTest (mock GPU ok): monkeypatch capable_nvidia_gpu -> (True, 24, "") and assert the Fish row then shows the NC-license disclosure + accept before any install control (D-08).
  4. Playwright-MCP: screenshot the shown-disabled Fish row + the license disclosure; a vision pass confirms wording/links.
  5. Confirm REQUIREMENTS.md HEAVY-03 + ROADMAP.md SC#3 read "shown but disabled" (the 05-02 reconciliation) so the verifier checks this behavior.
THEN USER (real GPU install + synthesis — REQUIRES an NVIDIA >=12 GB machine; almost certainly DEFERRED to 05-HUMAN-UAT.md, like the Phase-3 Windows UAT — not a defect):
  6. On a capable NVIDIA machine: accept the Fish license, confirm footprint, Install (shared torch venv or the Q-B fallback fish venv); it ends "Ready".
  7. Upload: select Fish + a custom or default voice, Convert a short TXT; confirm audio plays (verified by ear).
  8. Confirm the default install + Orpheus + F5 still work and no terminal was used.
  </how-to-verify>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q 2>&1 | tail -3 && /Users/tyler/Repos/diana/.venv/bin/python -c "import ast; ast.parse(open('diana/dashboard/pages/5_Settings.py').read()); ast.parse(open('diana/dashboard/pages/1_Upload.py').read()); print('syntax OK')" && grep -q 'shown but disabled' .planning/REQUIREMENTS.md && grep -q 'shown but disabled' .planning/ROADMAP.md</automated>
    <human-check>Without a capable GPU the Fish row is SHOWN-but-DISABLED with the VRAM reason (never hidden) and no download starts; with the GPU gate ok the NC-license disclosure precedes any install control; selecting Fish disables Convert with the GPU/install reason; (if run on NVIDIA) a real install ends "Ready" and Fish synthesizes — no terminal used.</human-check>
  </verify>
  <acceptance_criteria>
    - Automated pre-check passes (full suite green; both pages parse; the reconciled "shown but disabled" wording is present in REQUIREMENTS.md + ROADMAP.md) and the AppTest shown-disabled + license-gate assertions pass
    - Human confirms the shown-disabled + license behavior, OR the real GPU-install UAT is deferred with 05-HUMAN-UAT.md written (expected on non-NVIDIA machines — not a defect)
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (or "defer GPU UAT" with the UAT doc written — expected on non-NVIDIA machines).</resume-signal>
  <done>Human-verified (or GPU-install UAT deferred): Fish is shown-but-disabled-with-reason without a GPU (D-10), behind the GPU gate the NC-license accept-once + install flow works, the D-16 fail-fast covers Fish, and the reconciled wording is in place; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk text + ref clip/transcript → torch venv subprocess | gen_text + reference data cross into the fish worker |
| app → torch venv python + fish worker | the venv interpreter + bundled worker are executed |
| install → network (git+SHA fish-speech + HF weights) | multi-GB; behind the GPU + license + footprint gates |
| GPU detection → enable/disable | the gate decides whether installs/synthesis are even offered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering / Elevation | fish-speech git install + s2-pro weights (HIGH) | mitigate | Task 1/05-03: pinned git COMMIT SHA + pinned HF repo; install only into the venv; the D-08 license accept + D-04 footprint confirm + GPU gate are the human/host gates before any byte |
| T-05-CMD | Tampering / Elevation | synthesize subprocess (gen_text/ref_text) | mitigate | Task 1: stdin JSON, list argv, shell=False (text as data) |
| T-05-GPU | Tampering | GPU-gate bypass | mitigate | Task 1/2: initialize() AND the Settings row BOTH gate on capable_nvidia_gpu(); shown-but-disabled prevents install/use without a GPU (D-10) |
| T-05-IMP | DoS (bloat/perf) | cheap enumeration/badge/gate path | mitigate | Task 1/2: no torch/fish_speech import in fish_engine top, registry, or 5_Settings.py (grep + sys.modules) |
| T-05-LIC | Repudiation/Compliance | NC license not surfaced | mitigate | Task 2: accept-once disclosure (Fish Audio Research License / CC-BY-NC-SA-4.0) before any download, persisted (D-08) |
| T-05-EXE | Tampering | torch venv python + fish worker path | mitigate | Task 1: paths from venvs_dir()/heavy_worker() (not PATH); worker is package-data, never imported by the app |
</threat_model>

<verification>
- `/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q` green: test_fish_engine now PASS — ALL Wave-0 scaffolds landed; 0 failures.
- Cheap path imports no torch/fish_speech (sys.modules + grep). The Fish row is shown-but-disabled-with-reason without a capable GPU.
- Human-verify (Task 3) approved (shown-disabled + license paths) or GPU-install UAT deferred with the UAT doc.
</verification>

<success_criteria>
- HEAVY-03 delivered: Fish S2 Pro is an opt-in heavy engine, shown-but-disabled-with-reason without a capable GPU (D-10), behind the accept-once NC-license gate (D-08), reusing the engine-agnostic Custom Voices + a bundled default (A6).
- The three-engine lineup is complete (D-01); D-16 fail-fast + D-02 (no torch on the cheap path) hold; the reconciled HEAVY-03/SC#3 wording matches the implemented behavior.
</success_criteria>

<output>
Create `.planning/phases/05-heavy-opt-in-engines/05-07-SUMMARY.md` when done.
</output>
