---
phase: 05-heavy-opt-in-engines
plan: 05
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - diana/tts/f5_engine.py
  - diana/tts/heavy_workers/f5_worker.py
  - diana/data/voices/f5_default.wav
  - diana/data/voices/f5_default.txt
  - diana/tts/registry.py
  - diana/dashboard/pages/1_Upload.py
  - diana/dashboard/pages/5_Settings.py
autonomous: false
requirements: [HEAVY-02]
user_setup:
  - service: huggingface
    why: "F5-TTS downloads the F5TTS_v1_Base checkpoint (CC-BY-NC weights) from a public ungated HF repo on install"
    env_vars: []

must_haves:
  truths:
    - "A user can install F5-TTS on demand and immediately synthesize with a bundled, license-clean default voice — install -> synthesize works out of the box (HEAVY-02, D-01, D-15)"
    - "Before any F5 download, an accept-once non-commercial license disclosure (CC-BY-NC) is shown and must be accepted; acceptance persists in app_settings so re-installs don't re-prompt (D-08)"
    - "F5 implements the TTSEngine Protocol with lazy SDK imports — torch is never imported in the app interpreter; synthesis runs out-of-process in the shared torch venv with ref clip + transcript + gen text passed as data (D-17, T-05-CMD)"
    - "Choosing F5 when not installed disables Convert with the actionable prompt established in 05-04 (D-16)"
    - "The default voice clip ships as package-data with a known, license-clean provenance and its exact transcript (D-15, RESEARCH Q-E)"
  artifacts:
    - path: "diana/tts/f5_engine.py"
      provides: "F5Engine (TTSEngine, bundled default voice, subprocess synth into torch venv) + f5_install_spec"
      contains: "class F5Engine"
      min_lines: 60
    - path: "diana/tts/heavy_workers/f5_worker.py"
      provides: "venv-run worker: stdin JSON -> F5TTS().infer -> WAV; --prefetch mode"
      contains: "F5TTS"
    - path: "diana/data/voices/f5_default.txt"
      provides: "Exact transcript of the bundled default reference clip (D-15)"
    - path: "diana/data/voices/f5_default.wav"
      provides: "License-clean bundled default reference voice clip (D-15)"
  key_links:
    - from: "diana/dashboard/pages/5_Settings.py (F5 row)"
      to: "diana/tts/heavy_install.py::license_accepted / accept_license"
      via: "accept-once NC-license gate BEFORE the Install button (D-08)"
      pattern: "license_accepted|accept_license"
    - from: "diana/tts/f5_engine.py::synthesize"
      to: "diana/tts/heavy_workers/f5_worker.py (via torch venv python)"
      via: "subprocess with ref_file/ref_text/gen_text JSON, HF_HOME env, text as data"
      pattern: "ref_file|ref_text|gen_text"
---

<objective>
## Phase Goal

**As a** power user, **I want to** install F5-TTS on demand, accept its non-commercial license before download, and immediately narrate with a built-in default voice, **so that** I get high-quality neural cloning without a terminal and with the licensing made clear up front.

This slice delivers the F5 core: install -> select -> synthesize with a bundled, license-clean default voice (D-15), gated by the accept-once NC-license disclosure (D-08, the first license gate, reused by Fish in 05-07). Custom-voice cloning (recording/uploading your own reference) is the NEXT slice (05-06) — this slice proves F5 works end-to-end with the default voice first.

F5 reuses the heavy-engine substrate: the shared torch venv (D-03), the out-of-process worker pattern, and the Settings install row from 05-04 — extended here with the license gate. torch lives ONLY in `heavy_workers/f5_worker.py` (run by the venv python); the app interpreter never imports it.

Purpose: deliver the HEAVY-02 core + establish the license gate.
Output: F5 installable behind the license gate, selectable, synthesizing with the bundled default; `test_f5_engine` + `test_license_gate` green; UI human-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-heavy-opt-in-engines/05-CONTEXT.md
@.planning/phases/05-heavy-opt-in-engines/05-RESEARCH.md
@.planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md
@.planning/phases/05-heavy-opt-in-engines/05-03-SUMMARY.md

<interfaces>
<!-- Reuse directly. -->

diana/tts/orpheus_engine.py (05-04): the engine shape to clone (class + cheap initialize + _subprocess_synth + install_spec)
diana/tts/heavy_install.py (05-03): HeavyInstallSpec; install_engine(spec, state); license_accepted(db, engine); accept_license(db, engine)
diana/tts/install_state.py (05-02): heavy_engine_installed("f5") -> bool (maps to the shared "torch" venv); uninstall_heavy_engine("f5")
diana/dashboard/pages/5_Settings.py (05-04): _render_heavy_engine_row(engine, spec[, license]) ; _render_download_progress (two-phase) ; _cross_engine_badge ; "Heavy opt-in engines" subsection
diana/dashboard/pages/1_Upload.py (05-04): the D-16 fail-fast Convert gate is already generic (heavy_engine_failfast covers f5) — F5 only needs an _engine_readiness branch
diana/paths.py (05-02): venvs_dir()/"torch" ; hf_cache_dir() ; heavy_worker("f5_worker.py") ; the bundled clip is a PACKAGE resource (importlib.resources.files("diana.data").joinpath("voices","f5_default.wav"))

RESEARCH F5 facts (confirm pins from 05-03 SUMMARY):
  package: f5-tts==1.1.20 (pulls torch/torchaudio/vocos/transformers) ; weights SWivid/F5-TTS F5TTS_v1_Base (CC-BY-NC)
  F5TTS(model="F5TTS_v1_Base", device, hf_cache_dir).infer(ref_file, ref_text, gen_text, speed=1.0, remove_silence=True) -> (wav, sr, spec)
  ref clip ~2-12s + user transcript ; license link: https://github.com/SWivid/F5-TTS (weights CC-BY-NC)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: F5Engine + worker + bundled default voice + registry/Upload wiring</name>
  <files>diana/tts/f5_engine.py, diana/tts/heavy_workers/f5_worker.py, diana/data/voices/f5_default.wav, diana/data/voices/f5_default.txt, diana/tts/registry.py, diana/dashboard/pages/1_Upload.py</files>
  <behavior>
    - F5Engine.list_voices() returns the bundled default voice (id "f5_default"); enumeration imports NO torch (sys.modules assertion)
    - initialize() raises FileNotFoundError naming "Settings ▸ Voices" when not installed
    - synthesize() resolves (ref_file, ref_text) for the voice and builds the worker subprocess JSON {ref_file, ref_text, gen_text, out, hf_cache}
    - registry: "f5" in list_engines(); routes to F5Engine; cheap path imports no torch
    - Upload: an F5 readiness badge renders; the existing generic fail-fast already gates Convert
  </behavior>
  <read_first>
    - diana/tts/orpheus_engine.py (the 05-04 sibling to clone — class shape, _subprocess_synth, install_spec)
    - diana/tts/native_os_engine.py (lines 199-233 subprocess discipline; the engine to use for generating the default clip on-device)
    - diana/tts/registry.py (full — add f5 across the seams, mirroring the orpheus branch from 05-04)
    - diana/dashboard/pages/1_Upload.py (lines 43-82 _engine_readiness)
    - .planning/phases/05-heavy-opt-in-engines/05-RESEARCH.md (Code Examples F5 worker; Pitfall 7/8; Q-E bundled default; "Don't Hand-Roll" soundfile)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md (f5_engine + registry::_piper_voices dynamic merge + heavy_workers sections)
    - tests/test_f5_engine.py (the scaffold this flips green)
  </read_first>
  <action>
Generate the bundled default voice (D-15, RESEARCH Q-E): synthesize a fixed neutral public-domain sentence (~8-10 s) ON-DEVICE with an existing engine (native_os `say` on macOS is simplest and license-clean by construction; Kokoro/Piper acceptable) and save it as `diana/data/voices/f5_default.wav`; write the EXACT sentence to `diana/data/voices/f5_default.txt` (this is the reference transcript). The clip's provenance is license-clean (self-generated on-device, no third-party rights). Flag it at the Task 3 checkpoint for human confirmation per Q-E (the user may swap in a preferred public-domain/self-recorded clip + transcript).

Create `diana/tts/f5_engine.py` cloning `OrpheusEngine` (module top: stdlib + `from diana.tts.base import TTSVoice` only — NO torch/f5_tts). `class F5Engine` with `name = "f5"`; a static `VOICES = [TTSVoice("f5_default", "Default (F5)", "en-us", "neutral", tier="enhanced")]` (custom voices are merged in 05-06); `list_voices()` returns `list(self.VOICES)`. `initialize()` cheap: raise `FileNotFoundError("F5-TTS not installed — open Settings ▸ Voices and click Install.")` when `not install_state.heavy_engine_installed("f5")`. A `_resolve_ref(voice) -> (ref_file, ref_text)` mapping: for `"f5_default"` return the bundled clip path (`importlib.resources.files("diana.data").joinpath("voices","f5_default.wav")` -> str) + the bundled transcript text; (custom-voice ids are added in 05-06). `async synthesize(text, voice="f5_default", speed=1.0) -> bytes` -> `run_in_executor(self._subprocess_synth, ...)`; `_subprocess_synth` resolves the torch venv python (`venvs_dir()/"torch"/...`), the worker (`heavy_worker("f5_worker.py")`), a temp WAV out, the (ref_file, ref_text) for the voice, then `subprocess.run([vpy, worker], input=json.dumps({"ref_file","ref_text","gen_text": text,"out","speed", "hf_cache": str(hf_cache_dir())}), text=True, capture_output=True, timeout=600, env={**os.environ,"HF_HOME": str(hf_cache_dir())})`; raise on non-zero; read+unlink the WAV (text as stdin DATA, list argv — T-05-CMD). Add `f5_install_spec() -> HeavyInstallSpec` (engine="f5", venv_name="torch", packages the confirmed f5-tts pin, extra_index None, prefetch_argv=[heavy_worker("f5_worker.py"),"--prefetch"], estimate deps/weights bytes — torch + F5TTS_v1_Base; exact totals read live).

Create `diana/tts/heavy_workers/f5_worker.py` (run BY the torch venv — imports f5_tts/soundfile freely). `--prefetch`: construct `F5TTS(model="F5TTS_v1_Base", hf_cache_dir=os.environ.get("HF_HOME"))` once to download the checkpoint, exit 0. Else read JSON from stdin, `from f5_tts.api import F5TTS; f5 = F5TTS(model="F5TTS_v1_Base", device=req.get("device"), hf_cache_dir=req["hf_cache"]); wav, sr, _ = f5.infer(ref_file=req["ref_file"], ref_text=req["ref_text"], gen_text=req["gen_text"], speed=req.get("speed",1.0), remove_silence=True); import soundfile as sf; sf.write(req["out"], wav, sr, format="WAV")`. No `__init__.py` in heavy_workers (already absent).

In `diana/tts/registry.py`: add `"f5"` to `list_engines()` (after orpheus); add the lazy `_get_engine_class` + `create_engine` branches (construct `F5Engine()`); `get_engine_voices("f5")` uses the static-VOICES default branch for now (05-06 swaps it to a dynamic bundled+custom branch). Confirm `_get_engine_class("f5")` does not pull torch at module top so `all_engine_voices` stays heavy-free.

In `diana/dashboard/pages/1_Upload.py`: extend `_engine_readiness` with an `f5` branch (`heavy_engine_installed("f5")` -> "Ready — F5-TTS installed" else "~1.5 GB+ (torch + model), install in Settings ▸ Voices"). The Convert fail-fast is already generic (no change needed).
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/test_f5_engine.py -x -q && /Users/tyler/Repos/diana/.venv/bin/python -c "import diana.tts.registry as r, sys; r.get_engine_voices('f5'); assert 'f5' in r.list_engines(); assert 'torch' not in sys.modules" && /Users/tyler/Repos/diana/.venv/bin/python -c "import soundfile as sf; print(sf.info('diana/data/voices/f5_default.wav').duration)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_f5_engine.py` flips skip->PASS; `grep -nE "import torch|import f5_tts" diana/tts/f5_engine.py` returns nothing
    - `diana/data/voices/f5_default.wav` is a valid WAV (soundfile reads it; duration ~6-12 s) and `diana/data/voices/f5_default.txt` is non-empty (the exact transcript)
    - `'f5' in registry.list_engines()`; `registry.get_engine_voices('f5')` returns the bundled default; cheap path leaves `torch` out of `sys.modules`
    - `diana/tts/heavy_workers/f5_worker.py` contains `F5TTS` + a `--prefetch` branch; passes ref_file/ref_text/gen_text via stdin JSON
    - `1_Upload.py` `_engine_readiness` has an `f5` branch (grep `f5` present in the function)
  </acceptance_criteria>
  <done>F5Engine + worker + the bundled default voice exist with no torch on the cheap path, F5 registers and routes, and the Upload badge renders; the engine scaffold is green.</done>
</task>

<task type="auto">
  <name>Task 2: Settings F5 install row with the accept-once NC-license gate (D-08)</name>
  <files>diana/dashboard/pages/5_Settings.py</files>
  <read_first>
    - diana/dashboard/pages/5_Settings.py (the _render_heavy_engine_row + license-gate area added in 05-04; _cross_engine_badge; the "Heavy opt-in engines" subsection)
    - diana/tts/heavy_install.py (license_accepted / accept_license signatures)
    - diana/dashboard/pages/1_Upload.py (lines 224-235 — the dismissible-hint accept-once idiom to mirror for the license gate)
    - diana/tts/f5_engine.py (f5_install_spec — from Task 1)
    - .planning/phases/05-heavy-opt-in-engines/05-PATTERNS.md (5_Settings.py section — "License gate (D-08)")
  </read_first>
  <action>
Extend `_render_heavy_engine_row` (from 05-04) to accept an optional license descriptor (engine key + disclosure text + license URL). When a license is required and `heavy_install.license_accepted(config.storage.database_path, engine)` is False, render — BEFORE any install control or download — the NC-license disclosure naming "non-commercial / personal use only" with a link to the actual license, plus an "I accept" button that calls `heavy_install.accept_license(...)` and `st.rerun()` (the 1_Upload.py:224-235 dismissible-hint idiom). Only once accepted does the footprint-confirm + has_space + Install flow appear (D-08: acceptance precedes any byte; persisted so re-installs don't re-prompt).

Invoke the row for F5 in the "Heavy opt-in engines" subsection: `_render_heavy_engine_row("f5", f5_engine.f5_install_spec(), license={"text": "F5-TTS weights are released under CC-BY-NC (non-commercial / personal use only).", "url": "https://github.com/SWivid/F5-TTS"})`. Add an `f5` branch to `_cross_engine_badge` (heavy_engine_installed("f5") -> Ready, else "~1.5 GB+, downloads on install" — cheap probe, no torch import). Keep the existing uninstall control wired (engine uninstall reuses `install_state.uninstall_heavy_engine` from 05-04; for the shared torch venv, removing F5 must not delete the venv while Fish is installed — the install_state rule handles this).
  </action>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -c "import ast; ast.parse(open('diana/dashboard/pages/5_Settings.py').read())" && /Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `5_Settings.py` parses and the full suite stays green (0 failures)
    - `grep -n "license_accepted\|accept_license" diana/dashboard/pages/5_Settings.py` present (the gate consults + sets the accept-once flag)
    - `grep -n "non-commercial" diana/dashboard/pages/5_Settings.py` present; the F5 row passes a license URL (https://github.com/SWivid/F5-TTS)
    - `grep -n "f5_install_spec" diana/dashboard/pages/5_Settings.py` present (no hardcoded F5 repo IDs in the page)
    - No torch import added: `grep -nE "import torch|import f5_tts" diana/dashboard/pages/5_Settings.py` returns nothing
  </acceptance_criteria>
  <done>The Settings F5 row shows the CC-BY-NC accept-once license disclosure before any download, then the footprint-confirm + disk-check + two-phase install; the badge reflects state — all cheap, no torch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: human-verify the F5 slice + confirm the bundled default voice</name>
  <files>diana/dashboard/pages/5_Settings.py, diana/dashboard/pages/1_Upload.py, diana/data/voices/f5_default.wav</files>
  <action>
Pause for human verification AFTER Tasks 1-2 — no new feature code here. Run the agent PRE-CHECK FIRST (AppTest interaction tests + Playwright-MCP screenshots inspected by a vision agent — the active UI policy), fix any defect in f5_engine.py / registry.py / 5_Settings.py / 1_Upload.py and re-run, THEN hand to the user. A real multi-GB torch install/synthesize is environment-dependent: if it is not feasible on the verifying machine, write the steps into .planning/phases/05-heavy-opt-in-engines/05-HUMAN-UAT.md and resume (deferred UAT, not a defect). Also surface the bundled default clip for the Q-E provenance confirmation.
  </action>
  <what-built>
F5Engine (out-of-process synth in the shared torch venv, bundled default voice), the Settings F5 install row with the accept-once CC-BY-NC license gate shown BEFORE any download, the Upload readiness badge, and the generic D-16 fail-fast (from 05-04) covering F5.
  </what-built>
  <how-to-verify>
PRE-CHECK FIRST (agent, automatable):
  1. AppTest: 5_Settings.py Voices tab — assert the F5 row shows the non-commercial license disclosure + "I accept" and NO install control before acceptance; after accept (set the app_settings flag), assert the footprint-confirm + Install appear (D-08).
  2. AppTest: 1_Upload.py — select F5; assert Convert is disabled with the fail-fast message while F5 is uninstalled (D-16, reused from 05-04).
  3. Playwright-MCP: screenshot the F5 license disclosure + the Upload badge; a vision pass confirms wording/links render.
  4. Listen to `diana/data/voices/f5_default.wav` (or inspect duration/format) and read `f5_default.txt` — confirm the clip is intelligible, ~6-12 s, and the transcript matches.
THEN USER (real install + synthesis — may be deferred to 05-HUMAN-UAT.md if a multi-GB torch install is not feasible on the verifying machine):
  5. Settings ▸ Voices: F5 row — read + accept the license, confirm the footprint, Install; the two-phase progress ends at "Ready"; re-opening the row does NOT re-prompt the license (accept-once persisted, D-08).
  6. Upload: select F5 + the default voice, Convert a short TXT; confirm audio plays (verified by ear) and uses the F5 default voice.
  7. Confirm the default install (native_os/Kokoro/Piper) still works unchanged and no terminal was used.
  8. CONFIRM (Q-E): the bundled default clip's provenance is acceptable; if you prefer, replace `f5_default.wav` + `f5_default.txt` with a public-domain/self-recorded clip.
  </how-to-verify>
  <verify>
    <automated>/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q 2>&1 | tail -3 && /Users/tyler/Repos/diana/.venv/bin/python -c "import ast; ast.parse(open('diana/dashboard/pages/5_Settings.py').read()); ast.parse(open('diana/dashboard/pages/1_Upload.py').read()); import soundfile as sf; assert sf.info('diana/data/voices/f5_default.wav').duration > 1; print('OK')"</automated>
    <human-check>The F5 license disclosure shows before any install control and accept-once persists; selecting uninstalled F5 disables Convert; the bundled default clip is intelligible with a matching transcript; (if run) a real install ends "Ready" and F5 synthesizes the default voice — no terminal used.</human-check>
  </verify>
  <acceptance_criteria>
    - Automated pre-check passes (full suite green; both pages parse; the bundled clip reads with duration > 1 s) and the AppTest license-gate + fail-fast assertions pass
    - Human confirms the F5 install (behind the license) + default-voice synth slice works AND the bundled clip's Q-E provenance, OR the real-install UAT is deferred with 05-HUMAN-UAT.md written
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (or "defer install UAT" with the UAT doc written).</resume-signal>
  <done>Human-verified (or install-UAT deferred): the F5 license gate (accept-once), the bundled default voice (Q-E confirmed), the install row, and the D-16 fail-fast all work; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk text + ref clip/transcript → torch venv subprocess | gen_text + reference data cross into a subprocess |
| app → torch venv python + worker | the shared torch venv interpreter + bundled worker are executed |
| install → network (PyPI torch + HF weights) | multi-GB; behind the license + footprint gates |
| license acceptance → app_settings | a durable flag governs whether downloads may start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering / Elevation | f5-tts + torch install (HIGH) | mitigate | Pinned exact versions (05-03 Task 1); PyPI only; the D-08 license accept + D-04 footprint confirm are the human gates before any byte |
| T-05-CMD | Tampering / Elevation | synthesize subprocess (gen_text/ref_text) | mitigate | Task 1: ref_file/ref_text/gen_text passed as stdin JSON, list argv, shell=False |
| T-05-PATH | Tampering | bundled default clip path | mitigate | Task 1: the default clip is a fixed package resource resolved via importlib.resources (not user input); custom-clip path safety is handled in 05-06 |
| T-05-IMP | DoS (bloat/perf) | cheap enumeration/badge path | mitigate | Task 1/2: no torch/f5_tts import in f5_engine module top, registry, or 5_Settings.py (sys.modules + grep gates) |
| T-05-LIC | Repudiation/Compliance | NC license not surfaced | mitigate | Task 2: accept-once disclosure shown before any download, persisted (D-08); names non-commercial + links the license |
| T-05-SRC | DoS (app crash) | install thread touching st.* | mitigate | Reuses heavy_install.install_engine (state-only) + @st.fragment (05-03/05-04) |
</threat_model>

<verification>
- `/Users/tyler/Repos/diana/.venv/bin/python -m pytest tests/ -q` green: test_f5_engine + test_license_gate now PASS; Fish/custom_voices scaffolds still skip.
- Cheap path imports no torch/f5_tts (sys.modules + grep). The bundled default clip is a valid WAV with a matching transcript.
- Human-verify (Task 3) approved (incl. the Q-E provenance confirm) or install-UAT deferred with the UAT doc.
</verification>

<success_criteria>
- HEAVY-02 core delivered: F5 installs behind the accept-once NC-license gate (D-08), enumerates a license-clean bundled default voice (D-15), and synthesizes out-of-process with the default voice.
- The license gate is generic (Fish reuses it in 05-07); D-16 fail-fast covers F5; D-02 holds (no torch on the cheap path).
</success_criteria>

<output>
Create `.planning/phases/05-heavy-opt-in-engines/05-05-SUMMARY.md` when done.
</output>
