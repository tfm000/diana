---
phase: 03-native-os-tts-new-default
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - diana/tts/base.py
  - diana/tts/native_voices_macos.py
  - tests/test_native_voices_macos.py
autonomous: true
requirements: [NATIVE-03, NATIVE-05]
must_haves:
  truths:
    - "TTSVoice carries descriptive attributes (tier + bilingual) and existing kokoro/piper VOICES lists still construct without edits"
    - "The macOS say -v '?' output parses into TTSVoice objects, including parenthetical and non-ASCII voice names"
    - "Known macOS voices are prelabelled with a quality tier (novelty/compact/enhanced)"
  artifacts:
    - path: "diana/tts/base.py"
      provides: "Extended TTSVoice dataclass with tier + bilingual fields (trailing defaults)"
      contains: "tier"
    - path: "diana/tts/native_voices_macos.py"
      provides: "say -v '?' parser + curated prelabel map; enumerate_macos_voices() -> list[TTSVoice]"
      exports: ["enumerate_macos_voices"]
      min_lines: 40
  key_links:
    - from: "diana/tts/native_voices_macos.py"
      to: "diana.tts.base.TTSVoice"
      via: "import + construct with tier/bilingual"
      pattern: "from diana.tts.base import TTSVoice"
    - from: "diana/tts/native_voices_macos.py"
      to: "/usr/bin/say"
      via: "list-argv subprocess.run with timeout"
      pattern: "subprocess.run\\(\\[.*say"
---

<objective>
First vertical slice of the macOS native engine: the **data layer**. Extend `TTSVoice` with the descriptive attributes the picker needs (D-05), and build the `say -v '?'` parser + curated prelabel map that turns the OS voice catalog into `list[TTSVoice]` (D-04, D-06). Pure, fully unit-testable against the Wave 0 fixture — no engine, no subprocess in unit tests.

Purpose: NATIVE-03 (dynamic enumeration) and NATIVE-05 (attributes) both rest on this. Factoring the parser into its own module keeps the engine file lean and the regex/prelabel logic testable from a captured fixture string (RESEARCH "Macros for testability").
Output: extended `diana/tts/base.py` `TTSVoice`; new `diana/tts/native_voices_macos.py`; the macOS-parser tests in `tests/test_native_voices_macos.py` turned green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-os-tts-new-default/03-RESEARCH.md
@.planning/phases/03-native-os-tts-new-default/03-PATTERNS.md

@diana/tts/base.py
@tests/fixtures/say_voices.txt

<interfaces>
<!-- Current TTSVoice (diana/tts/base.py:5-11) — extend in place, append defaulted fields -->
@dataclass
class TTSVoice:
    id: str
    name: str
    language: str
    gender: str

<!-- Positional consumers that MUST stay valid (do NOT edit these) -->
diana/tts/kokoro_engine.py:14-24  KokoroEngine.VOICES  → TTSVoice("af_heart","Heart (Female)","en-us","female")  (4 positional args)
diana/tts/piper_engine.py:23-29   PiperEngine.VOICES   → TTSVoice("en_US-lessac-medium","Lessac (US Medium)","en-us","male")  (4 positional args)

<!-- VERIFIED say -v '?' line format (RESEARCH Pattern 5): <name><2+sp><locale><2+sp># <sample> -->
<!-- locale token: [a-z]{2,3}(?:[_-][A-Za-z0-9]{2,4})?  e.g. en_US, fr_CA, yue_CN -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TTSVoice with tier and bilingual</name>
  <files>diana/tts/base.py</files>
  <read_first>
    - diana/tts/base.py (the file being modified — `TTSVoice` dataclass at lines 5-11)
    - diana/tts/kokoro_engine.py (lines 14-24: `VOICES` built with 4 positional args — must stay valid)
    - diana/tts/piper_engine.py (lines 23-29: `VOICES` built with 4 positional args — must stay valid)
    - .planning/phases/03-native-os-tts-new-default/03-RESEARCH.md ("TTSVoice extension" code example; D-05 attribute list)
    - .planning/phases/03-native-os-tts-new-default/03-PATTERNS.md (`base.py` MODIFY section — exact extension + the trailing-default mandate)
  </read_first>
  <behavior>
    - A `TTSVoice` constructed with 4 positional args (id, name, language, gender) still works and gets `tier="standard"`, `bilingual=False` by default (kokoro/piper VOICES unchanged).
    - A `TTSVoice` can be constructed with `tier=` and `bilingual=` keyword args.
  </behavior>
  <action>
    Append two trailing defaulted fields to the `TTSVoice` dataclass (per D-05): `tier: str = "standard"` (allowed values, by convention: "novelty" | "compact" | "enhanced" | "standard"/"neural") and `bilingual: bool = False` (multilingual/bilingual flag). Defaults are MANDATORY and MUST be trailing so the positional `KokoroEngine.VOICES`/`PiperEngine.VOICES` constructions stay valid with zero edits. Add a short inline comment on each new field per the project's comment convention. Do NOT add a Protocol method here (the optional `default_voice()` Protocol addition lands in Plan 03 with the engine). Do NOT edit the kokoro/piper VOICES lists.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_native_voices_macos.py::test_voice_has_tier tests/test_piper_engine.py tests/test_tts_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from diana.tts.base import TTSVoice; v=TTSVoice('a','A','en-us','female'); assert v.tier=='standard' and v.bilingual is False"` exits 0
    - `.venv/bin/python -c "from diana.tts.kokoro_engine import KokoroEngine; from diana.tts.piper_engine import PiperEngine; assert KokoroEngine.VOICES and PiperEngine.VOICES"` exits 0 (positional constructions still valid)
    - `tests/test_native_voices_macos.py::test_voice_has_tier` passes
  </acceptance_criteria>
  <done>TTSVoice has tier + bilingual with trailing defaults; kokoro/piper VOICES construct unchanged; test_voice_has_tier green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the macOS say-voices parser and prelabel map</name>
  <files>diana/tts/native_voices_macos.py, tests/test_native_voices_macos.py</files>
  <read_first>
    - diana/processing/cleaner.py (the structural analog — module-level `_SCREAMING_SNAKE_CASE` compiled-regex/set constants + pure helper functions; module docstring style)
    - diana/tts/piper_engine.py (lines 113-122: list-argv `subprocess.run([...], timeout=...)`, never `shell=True` — the V5 security pattern to copy)
    - .planning/phases/03-native-os-tts-new-default/03-RESEARCH.md (Pattern 5: VERIFIED `_SAY_LINE` regex `^(?P<name>.+?)\s+(?P<locale>[a-z]{2,3}(?:[_-][A-Za-z0-9]{2,4})?)\s+#\s*(?P<sample>.*)$`; novelty seed set; `base = name.split(" (")[0]`; locale `en_US`→`en-us`; Anti-pattern: never `.split()`)
    - .planning/phases/03-native-os-tts-new-default/03-PATTERNS.md (`native_voices_macos.py` NEW section — exact structure to copy; testability seam)
    - tests/fixtures/say_voices.txt (the deterministic parser input)
    - tests/test_native_voices_macos.py (the file being modified — Wave 0 scaffolds to turn green)
  </read_first>
  <behavior>
    - test_parse_say_output: every non-blank line in the fixture maps to a TTSVoice; a parenthetical-name line (e.g. "Eddy (English (US))") parses with the full name retained and the locale extracted via the regex (NOT `.split()`).
    - test_tier_classification: a known novelty name (e.g. "Zarvox", "Bells", "Bad News") → tier "novelty"; a known enhanced name (e.g. "Alex") → "enhanced"; any other preinstalled voice → "compact" (the D-06 fallback).
    - test_enumerate_real_say (macOS smoke): `enumerate_macos_voices()` shells real `say` and returns ≥1 voice.
  </behavior>
  <action>
    Create `diana/tts/native_voices_macos.py` mirroring `cleaner.py`'s structure: module docstring; `import re`, `import subprocess`, `from diana.tts.base import TTSVoice`. Define the module-level compiled `_SAY_LINE` regex (the VERIFIED pattern from RESEARCH Pattern 5) and curated prelabel sets `_NOVELTY` and `_ENHANCED` (fill the full novelty set from RESEARCH's observed list: Bad News, Bahh, Bells, Boing, Bubbles, Cellos, Good News, Jester, Organ, Trinoids, Whisper, Wobble, Zarvox, Albert, plus the others RESEARCH lists; `_ENHANCED = {"Alex"}` seed). Provide a pure parse step (a module-level helper that takes the `say -v '?'` text and returns `list[TTSVoice]`) so the unit test feeds the fixture string with no subprocess, AND the public `enumerate_macos_voices() -> list[TTSVoice]` that shells `subprocess.run(["/usr/bin/say", "-v", "?"], capture_output=True, text=True, timeout=15)` (list-argv, never `shell=True` — V5) and delegates to the pure parser. For each matched line: `base = name.split(" (")[0]`; `locale = m.group("locale").replace("_","-").lower()`; `tier = "novelty" if base in _NOVELTY else "enhanced" if base in _ENHANCED else "compact"`; append `TTSVoice(id=name, name=name, language=locale, gender="unknown", tier=tier, bilingual=False)` (`say -v <name>` accepts the display name as the id; gender is "unknown" — `say` does not expose it). Then turn the three Wave 0 parser tests green by importing the real symbols and removing their xfail/skip guards.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_native_voices_macos.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_native_voices_macos.py::test_parse_say_output`, `::test_tier_classification`, `::test_voice_has_tier`, and `::test_enumerate_real_say` all pass (no longer xfail)
    - `.venv/bin/python -c "from diana.tts.native_voices_macos import enumerate_macos_voices; vs=enumerate_macos_voices(); assert len(vs)>=1 and all(hasattr(v,'tier') for v in vs)"` exits 0
    - `grep -Eq 'subprocess.run\(\[' diana/tts/native_voices_macos.py` AND `grep -Lq 'shell=True' diana/tts/native_voices_macos.py` (list-argv, never shell=True)
    - The parser uses the regex, not `.split()`: `grep -q '_SAY_LINE' diana/tts/native_voices_macos.py`
  </acceptance_criteria>
  <done>Parser + prelabel map parse the fixture (incl. parenthetical names) into tiered TTSVoice objects; `enumerate_macos_voices()` shells `say` safely; all macOS-parser Wave 0 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS `say` CLI → Diana | Untrusted-length voice-catalog text crosses into the parser via subprocess stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering / Elevation | `enumerate_macos_voices()` `say -v '?'` subprocess | mitigate | List-argv `subprocess.run(["/usr/bin/say","-v","?"], ...)`, never `shell=True`; no document text or user input is passed to enumeration (V5). Copy `piper_engine.py` list-argv shape. |
| T-03-04 | Denial of Service | `say -v '?'` hung/slow process | mitigate | `timeout=15` on `subprocess.run` (enumeration is fast; bound it). |
| T-03-05 | Tampering | malformed/garbage `say` output line | accept | The `_SAY_LINE` regex skips non-matching lines (`if not m: continue`); a malformed line is dropped, not crashed on. Best-effort by design. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages (`say` is OS-provided; macOS needs zero new deps). N/A. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_native_voices_macos.py -q` all green.
- `.venv/bin/python -m pytest tests/test_tts_registry.py tests/test_config.py tests/test_piper_engine.py -q` still green (TTSVoice extension did not regress kokoro/piper).
- `enumerate_macos_voices()` returns ≥1 tiered voice on this Mac.
</verification>

<success_criteria>
- TTSVoice carries tier + bilingual; kokoro/piper VOICES unchanged and valid.
- `native_voices_macos.py` parses the captured fixture (parenthetical + locale handling) into tiered TTSVoice objects via the verified regex.
- `enumerate_macos_voices()` shells `say` via list-argv with a timeout; all macOS-parser Wave 0 tests turned green.
</success_criteria>

<output>
Create `.planning/phases/03-native-os-tts-new-default/03-02-SUMMARY.md` when done.
</output>
