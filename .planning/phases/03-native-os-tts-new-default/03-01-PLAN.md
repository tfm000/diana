---
phase: 03-native-os-tts-new-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_native_os_engine.py
  - tests/test_native_voices_macos.py
  - tests/fixtures/say_voices.txt
autonomous: true
requirements: [NATIVE-01, NATIVE-02, NATIVE-03, NATIVE-04, NATIVE-05]
must_haves:
  truths:
    - "The async test toolchain runs without erroring on @pytest.mark.asyncio (pytest-asyncio installed, asyncio_mode set)"
    - "Failing/xfail test scaffolds exist for every macOS-testable NATIVE behavior, ready to turn green as implementation lands"
    - "A captured say -v '?' fixture lets the macOS parser be tested deterministically without a subprocess"
  artifacts:
    - path: "tests/test_native_os_engine.py"
      provides: "Test scaffold: sys.platform dispatch, macOS synth (real+mock), WinRT branch (mock), SAPI5 flag, default-voice validation, filter/search"
      contains: "import"
    - path: "tests/test_native_voices_macos.py"
      provides: "Test scaffold: say -v '?' parser, tier classification, TTSVoice tier/bilingual fields"
      contains: "import"
    - path: "tests/fixtures/say_voices.txt"
      provides: "Captured say -v '?' output (parenthetical + non-ASCII names) for deterministic parser tests"
      min_lines: 30
    - path: "pyproject.toml"
      provides: "asyncio_mode=auto so @pytest.mark.asyncio tests run under strict-default pytest-asyncio"
      contains: "asyncio_mode"
  key_links:
    - from: "tests/test_native_voices_macos.py"
      to: "tests/fixtures/say_voices.txt"
      via: "read fixture text, feed to parser"
      pattern: "say_voices"
---

<objective>
Wave 0 of Phase 3 (the Nyquist validation contract). Stand up the test toolchain and failing/xfail scaffolds so every macOS-testable native-OS behavior has an automated `<automated>` target before any implementation lands. No production code changes — this plan only creates test files, a fixture, and fixes the async test runner.

Purpose: VALIDATION.md is the contract for this phase; it lists Wave 0 gaps that every later task depends on. `pytest-asyncio` is currently NOT installed in the `.venv` (verified) and `asyncio_mode` is unset, so `@pytest.mark.asyncio` tests error out (the same failure deferred from Phase 1). This plan removes that blocker.
Output: `tests/test_native_os_engine.py`, `tests/test_native_voices_macos.py`, `tests/fixtures/say_voices.txt`, and `asyncio_mode = "auto"` in `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-os-tts-new-default/03-VALIDATION.md
@.planning/phases/03-native-os-tts-new-default/03-RESEARCH.md
@.planning/phases/03-native-os-tts-new-default/03-PATTERNS.md

<interfaces>
<!-- Contracts the scaffold tests assert against. The named symbols do NOT exist yet
     (they land in Plans 02–05); these tests are RED/xfail until then. -->

TTSVoice (after Plan 02 extension) — diana/tts/base.py:
  @dataclass TTSVoice: id:str, name:str, language:str, gender:str, tier:str="standard", bilingual:bool=False

native_voices_macos (Plan 02) — diana/tts/native_voices_macos.py:
  enumerate_macos_voices() -> list[TTSVoice]          # shells `say -v '?'`
  (a pure string->list[TTSVoice] parse step the test feeds from the fixture)

NativeOSEngine (Plans 03/05) — diana/tts/native_os_engine.py:
  class NativeOSEngine: name = "native_os"
  initialize() / async synthesize(text, voice, speed=1.0) -> bytes
  list_voices() -> list[TTSVoice] / default_voice() -> str / shutdown()
  self._platform = sys.platform   # "darwin" | "win32"

registry (Plan 03) — diana/tts/registry.py:
  get_engine_voices("native_os") -> dynamic list (NOT cls.VOICES)
  list_engines() -> ["native_os", "kokoro", "piper"]
  engine_is_ascii_only("native_os") -> False

filter helper (Plan 04) — pure function, Streamlit-free:
  filter_voices(voices, language=None, tier=None, query=None) -> list[TTSVoice]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest-asyncio and set asyncio_mode</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (the file being modified — `[tool.pytest.ini_options]` at lines 43-45, `dev` extra at line 38 already declares `pytest-asyncio>=0.23.0`)
    - .planning/phases/03-native-os-tts-new-default/03-VALIDATION.md (Async note + Wave 0 checklist: "Confirm pytest-asyncio mode")
    - .planning/phases/01-foundation-privacy-toggle/deferred-items.md (the Phase-1 deferred failure: async tests error because pytest-asyncio absent)
  </read_first>
  <action>
    Install the already-declared dev dependency into the project venv: `.venv/bin/pip install "pytest-asyncio>=0.23.0"`. Then add `asyncio_mode = "auto"` to the existing `[tool.pytest.ini_options]` table in pyproject.toml (the table currently holds only `testpaths` and `pythonpath`). `auto` mode means `@pytest.mark.asyncio` is optional and bare `async def test_*` run as asyncio — but RESEARCH/VALIDATION still call for explicit markers, which remain valid under `auto`. Do NOT touch requirements.txt here (the winrt deps land in Plan 05; pytest-asyncio is a dev-only tool already in the `dev` extra).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import pytest_asyncio" && grep -q 'asyncio_mode = "auto"' pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import pytest_asyncio"` exits 0
    - `pyproject.toml` `[tool.pytest.ini_options]` contains the line `asyncio_mode = "auto"`
    - `.venv/bin/python -m pytest tests/ -q` collects without the "async def functions are not natively supported" error
  </acceptance_criteria>
  <done>pytest-asyncio importable in the venv; asyncio_mode=auto present; existing suite still collects and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the say -v '?' fixture</name>
  <files>tests/fixtures/say_voices.txt</files>
  <read_first>
    - .planning/phases/03-native-os-tts-new-default/03-RESEARCH.md (Pattern 5: verified output format `<name><2+ spaces><locale><2+ spaces># <sample>`; names with nested parens like "Eddy (English (US))" and non-ASCII like "Amélie"; 184 voices parse with zero failures)
    - .planning/phases/03-native-os-tts-new-default/03-PATTERNS.md (`tests/fixtures/say_voices.txt` analog = existing `tests/fixtures/cleaner/` dir)
  </read_first>
  <action>
    On this macOS box, capture the live enumeration into the fixture: `.venv/bin/python -c "import subprocess,pathlib; pathlib.Path('tests/fixtures/say_voices.txt').write_text(subprocess.run(['/usr/bin/say','-v','?'],capture_output=True,text=True,timeout=15).stdout)"`. The captured file MUST include at least one parenthetical-name line (e.g. an "(English (US))" voice) and ideally a non-ASCII name (e.g. "Amélie") so the parser test in Plan 02 exercises those edge cases. This is a committed fixture — deterministic input for the pure parser test (no subprocess in unit tests).
  </action>
  <verify>
    <automated>test -s tests/fixtures/say_voices.txt && grep -q '#' tests/fixtures/say_voices.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/say_voices.txt` exists and is non-empty (≥30 lines on a stock Mac)
    - At least one line contains a parenthesis in the name portion (verify: `grep -c '(' tests/fixtures/say_voices.txt` ≥ 1)
    - Every voice line contains the `#` sample delimiter
  </acceptance_criteria>
  <done>Fixture captured from real `say -v '?'`, includes parenthetical-name edge case, committed for deterministic parser tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED/xfail test scaffolds for the native engine and macOS parser</name>
  <files>tests/test_native_os_engine.py, tests/test_native_voices_macos.py</files>
  <read_first>
    - tests/test_piper_engine.py (the analog: `patch.dict("sys.modules", {"piper": MagicMock()})` to mock an absent SDK at lines 27-31; the force-ImportError idiom at lines 33-47; `TestListVoices` TTSVoice-field asserts at lines 86-103)
    - .planning/phases/03-native-os-tts-new-default/03-VALIDATION.md (Per-Task Verification Map — the exact test function names and automated commands this scaffold must define)
    - .planning/phases/03-native-os-tts-new-default/03-RESEARCH.md (Validation Architecture → Test Map; Macros for testability — split parser into a pure step, inject/mock the winrt module, factor `filter_voices` out of Streamlit)
    - tests/fixtures/say_voices.txt (the fixture Task 2 created — the macOS parser test reads it)
  </read_first>
  <behavior>
    Define these test functions (names are load-bearing — VALIDATION.md references them). Each imports a not-yet-existing symbol, so mark the whole module (or each test) `@pytest.mark.xfail(reason="implemented in Plan 0X", strict=False)` OR use `pytest.importorskip`/try-import guards so the suite stays GREEN in Wave 0 and the tests flip to real pass as implementation lands.

    tests/test_native_voices_macos.py:
      - test_parse_say_output: feed `tests/fixtures/say_voices.txt` text to the parser; assert every non-blank line → a TTSVoice; assert a parenthetical-name line parses (name retained, locale extracted). [NATIVE-03]
      - test_voice_has_tier: a parsed voice exposes `.tier` and `.bilingual`. [NATIVE-05]
      - test_tier_classification: a known novelty name ("Zarvox"/"Bells") → tier "novelty"; a plain voice → "compact". [NATIVE-05]
      - test_enumerate_real_say (macOS smoke, real subprocess): `enumerate_macos_voices()` returns ≥1 voice. [NATIVE-04]

    tests/test_native_os_engine.py:
      - test_macos_synthesize_returns_wav (mock subprocess or smoke): macOS branch returns bytes with a RIFF/WAVE header. [NATIVE-01]
      - test_macos_real_synth_smoke (macOS smoke, real `say`): synth a short string → non-empty, pydub-readable WAV. [NATIVE-04]
      - test_winrt_synth_reads_buffer (mock): with `self._platform="win32"` and a mocked `winrt.windows.media.speechsynthesis`, the WinRT branch calls the synth API and returns bytes via `bytearray(buffer)` (not DataReader). [NATIVE-02]
      - test_sapi5_only_flagged (mock): a voice list with no `OneCore` id sets the SAPI5-only flag (D-11). [NATIVE-02]
      - test_default_voice_validation: a remembered voice id absent from the live list is NOT preselected; falls back to engine default (D-03). [NATIVE-05]
      - test_voice_filter_search: pure `filter_voices(voices, language=, tier=, query=)` filters/searches correctly. [NATIVE-05]
  </behavior>
  <action>
    Create both test modules with the function signatures above. Import the (future) symbols from `diana.tts.native_os_engine`, `diana.tts.native_voices_macos`, and `diana.tts.registry`. Because none exist yet, guard collection so Wave 0 stays green: wrap module-level imports in `try/except ImportError` setting a `pytest.mark.skip`/`xfail` marker, OR mark each test `@pytest.mark.xfail(strict=False)`. Use the `patch.dict("sys.modules", {...})` mock-the-SDK pattern from `test_piper_engine.py` for the WinRT tests. Mark async tests `@pytest.mark.asyncio`. Do NOT modify `tests/test_tts_registry.py` or `tests/test_config.py` here (those known-break edits belong to Plan 03, in the same wave that causes them). Each test must have a real assertion body (not `pass`) so it becomes a meaningful gate once the symbol exists.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_native_os_engine.py tests/test_native_voices_macos.py -q 2>&1 | grep -Eq 'xfail|skip|passed' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_native_os_engine.py tests/test_native_voices_macos.py -q` exits 0 (collected as xfail/skip, no collection errors, no hard failures)
    - Both files define every test-function name listed in VALIDATION.md Per-Task Verification Map (verify: `grep -c "def test_" tests/test_native_voices_macos.py` ≥ 4 and `grep -c "def test_" tests/test_native_os_engine.py` ≥ 6)
    - `.venv/bin/python -m pytest -q` (full suite) exits 0 — Wave 0 introduces no red
  </acceptance_criteria>
  <done>Both scaffold modules exist with named, assertion-bearing xfail/skip tests covering NATIVE-01..05 macOS-testable behaviors; full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → test toolchain | `pip install` of a dev dependency (pytest-asyncio) into the venv |
| captured fixture → parser tests | `say -v '?'` stdout written to a committed file consumed by tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `pip install pytest-asyncio` | mitigate | Package already legitimacy-audited in Phase 1 `dev` extra (`pytest-asyncio>=0.23.0`); installed from PyPI; no new/unvetted package introduced. No blocking checkpoint needed — not an [ASSUMED]/[SUS]/[SLOP] package. |
| T-03-01 | Information Disclosure | `tests/fixtures/say_voices.txt` | accept | Fixture is the public OS voice catalog (no PII/secrets); committing it is standard test-fixture practice. |
| T-03-02 | Tampering | `say -v '?'` subprocess used to capture the fixture | mitigate | List-argv `subprocess.run(["/usr/bin/say","-v","?"], timeout=15)`, never `shell=True` (V5). One-shot capture command; no untrusted input. |
</threat_model>

<verification>
- `.venv/bin/python -c "import pytest_asyncio"` succeeds.
- `grep 'asyncio_mode = "auto"' pyproject.toml` matches.
- `tests/fixtures/say_voices.txt` is non-empty with a parenthetical-name line.
- `.venv/bin/python -m pytest -q` is green (scaffolds collected as xfail/skip).
</verification>

<success_criteria>
- pytest-asyncio installed; asyncio_mode=auto set; the Phase-1 async-test blocker is removed.
- All Wave 0 test scaffolds and the say fixture exist; every macOS-testable NATIVE behavior has a named test target referenced by VALIDATION.md.
- Full suite green; no production code touched.
</success_criteria>

<output>
Create `.planning/phases/03-native-os-tts-new-default/03-01-SUMMARY.md` when done.
</output>
